# 演讲比赛「一场一投」打分系统 —— 项目方案（PLAN）

> 2026-09-18 起，本项目从「一次性打完所有人」**完全替换**为**演讲比赛现场模式**。
> 旧模式的设计（11 名参赛者 × 5 维度一次性提交、不允许弃权、内网单机部署）已全部作废。
>
> 源码里的注释会写 `PLAN §x.y` 指向本文档对应小节，改结构时请同步改引用。

---

## 1. 项目背景与目标

一位演讲者上场就是一个时段（下称**一场**）：

- 一场之内，每位评委的手机上**只显示当前这位演讲者**的打分页，5 个维度各打 1–5 分。
- 提交后评委只看到等待页，**看不到任何其他演讲者的信息**，也没有上一位/下一位导航。
- 主持人（管理员）决定何时切到下一位；**随时可切**，未提交的评委记为**弃权**。

评委用手机在**企业微信**里点链接访问，因此部署形态是公网服务器而不是内网笔记本
（内网方案此前已判定存在 WiFi AP 客户端隔离的单点阻断风险）。

---

## 2. 锁定决策表

| # | 项目 | 决定 | 影响 |
|---|---|---|---|
| 1 | 推进模型 | 管理员**随时可切**，未提交的记为弃权 | 决定状态机，见 §7.2 |
| 2 | 计分 | **按每位评委的加权总分去一高一低**（《评选方案》口径），票数不足时降级；⚠️ 2026-09-18 由「每格各自去分」改为此口径，见 §6.2 | 见 §6 |
| 3 | 评委身份 | ⚠️ **选票记录登录码**：`ballot` 带 `code` 列，可以还原「谁给谁打了多少分」。**2026-09-18 由「强匿名」改为「可归属」**，见 §5.2 | 见 §5.2、§14 |
| 4 | 登录 | **统一入口 `/v` + 手输登录码**；码由后台**预先批量签发**，管理员一对一发放 | 见 §7.1 |
| 5 | 登录码备注 | **不加备注名**，管理员另存一份对应表 | 数据层少一列身份信息 |
| 6 | 评委人数 | 后台可配的**固定值**，作为「已收 X / N」的分母 | 上限 20，见 §6.4 |
| 7 | 彩排 | 提供**一键重置**（清场次/选票/**登录码**，保留名单/维度）；⚠️ 2026-09-22 起登录码一并删除失效，原先保留 | 见 §8.3 |
| 8 | 部署 | 腾讯云轻量应用服务器，公网 IP 直连，不备案 | 见 §3、§4 |
| 9 | 提交 | **每场终局**，不可修改；重开 = 为该演讲者新开一场 | 见 §5.4 |
| 10 | 平局 | 依次比四条同分顺位；四条全同才是真并列，下一名跳号，并由评委组投票 | 见 §6.3、§6.5 |
| 11 | 结果可见性 | 管理员随时可见；比赛期间靠横幅自觉不泄露 | 见 §8.4 |
| 12 | 数据导出 | 保留 Excel 导出（汇总 / 评委明细 / 维度明细 / 去分明细 / 计分说明五张表） | 见 §8.4 |
| 13 | 奖级 | 按名次表**位置**分配：一等奖 2 名、二等奖 3 名、其余三等奖 | 见 §6.6 |

---

## 3. ⚠️ Day-0：先验证服务器，再写代码

**这一步不通，后面全部作废。** 顺序：

1. 注册腾讯云 → **实名认证**
2. 购买**轻量应用服务器**，内地节点，最低配
3. 防火墙/安全组放行 TCP **3001**
4. 把现有版本直接部署上去，**先不改代码**
5. 实测三件事：
   - 手机 **4G/5G** 打开 `http://<公网IP>:3001/v`（暗号走请求头令牌时，这一步
     必须经公网映射/网关转发，它替客户端加上 `token` 头）—— 验证腾讯云没有拦非标准端口
   - **在企业微信里点这个链接** —— 验证企微没有弹风险提示或拦截页
   - 确认打开后落在**企微内置浏览器**还是**系统浏览器**（见 §12 R3）

**判定**：三项全过 → 按本方案走。**任一不过 → 立刻改买腾讯云香港节点**
（同样几步就能开，免备案、合法、无拦截风险；代价是跨境公网质量不保证，
但 20 人提交几百字节 JSON 对延迟不敏感，真正的风险是丢包，所以香港节点也必须当场实测）。

---

## 4. 部署方案

**选型**：腾讯云轻量应用服务器（Lighthouse），内地节点，公网 IP 直连，**不使用域名**。
不备案的前提下，域名指向内地服务器一定会被拦；直接用 `IP:端口` 不经过域名体系。
这属于灰色地带，所以 §3 的验证是前置条件。

**必须做的四处适配**：

1. **公网地址注入**。`src/net.js` 的 `entryUrls()` 在没有配置时用
   `os.networkInterfaces()` 拼地址 —— 在云服务器上那只会返回内网 IP（`10.x`），
   二维码会指向评委根本连不上的地址。所以设置环境变量 **`PFXT_PUBLIC_URL`**
   （如 `http://123.45.67.89:3001`），它优先返回；局域网地址排在后面仅供本机核对。
   **路径前缀会自动补上** —— 写 `http://1.2.3.4:3001` 与写
   `http://1.2.3.4:3001/<口令>` 等价，不会拼出 `/口令/口令/v`。
   （当前默认不启用路径前缀，这条只在切回 ① 时起作用。）
   启动横幅会明确打印实际生效的公网地址，便于一眼核对。
2. **常驻运行**。`pm2 start src/server.js --name pfxt` + `pm2 save` / `pm2 startup`。
   `start.sh` 保留作开发用。
3. **端口与信任代理**。`PORT=3001`、`HOST=0.0.0.0`；
   `app.set('trust proxy', false)` **不要改** —— 直连场景下 `req.ip` 就是真实来源 IP，限流依赖它。
4. **口令暗号**（见下节）。

### 4.1 口令暗号 —— 把后台藏起来

公网 IP 直连意味着：端口一旦放行，**`/admin` 就等于挂在公网上**。
登录口令是唯一的门，而门是可以被暴力尝试的（后台限流是 5 分钟 10 次，撑得住，
但没有理由让对方知道门在哪）。所以加一道**只有自己人知道的暗号**。

同一个口令值（`src/net.js` 里的那个常量）有**两种承载方式**，按部署环境二选一。
当前默认走 **②请求头**，URL 保持干净：

| | 承载方式 | 形态 | 开关 |
|---|---|---|---|
| ① | **路径前缀** | `http://<IP>:3001/<口令>/v` | `DEFAULT_BASE_PATH` / `PFXT_BASE_PATH` |
| ② | **请求头令牌** | `curl -H "token: <口令>" http://<IP>:3001/v` | `DEFAULT_API_TOKEN` / `PFXT_TOKEN` |

**配置在哪**：`src/net.js` 是唯一真源。要换口令，只改那一处（或设对应的
`PFXT_*` 环境变量）。两者可以同时开（那就两道都要过），也可以都关
（两个环境变量都设空串 —— 完全不设防，只应在纯内网调试时出现，服务启动时会告警）。

#### ② 请求头令牌的边界（改动时别踩）

- **只拦 GET / HEAD**（`src/net.js` 的 `isGuardedMethod`）。这是刻意的，理由：
  公网映射/网关代加请求头时只能盲加（它分不出导航请求与 XHR），但也可能存在
  按方法配置的网关。**只在 GET 上要求，这两种网关下都能跑通**；若要求所有方法，
  「只给 GET 加头」的网关下页面自己的提交就会被自己挡死。
  代价：POST / PUT 不在这层保护之内 —— 它们靠真正的鉴权兜底（管理端口令 + 会话，
  评委端登录码）。这道暗号本来就只是「不被发现」。
- ⚠️ **浏览器发不出自定义请求头。** 地址栏、扫码、链接点击、`<img>` 都不行。
  所以这个头**必须由公网映射/网关在转发时代加**。直接用浏览器开
  `http://ip:3001/v` 必然 404 —— 这不是 bug，是这道暗号的定义。自测只能用 curl
  或带改头插件。
- ⚠️ **页面自己的 GET 请求**（评委页 3 秒一次的 `/api/v/:code/state` 轮询、
  后台各列表页）**不带这个头**，同样要靠网关注入。若现场出现「页面能打开、
  但一直转圈/表格加载失败」，第一个要怀疑的就是网关只给导航请求加了头、
  没给 XHR 加。届时可让服务端把口令注入页面、由前端自己带上（此路可行，
  不额外泄露任何东西 —— 能打开页面的人本来就已持有口令），但目前**没有做**。
- **被挡下时回的 404 与「路径不存在」逐字相同**，不是 401/403 —— 回 401 等于
  告诉扫描器「这个头我认识，只是值不对」。拒绝原因只打在**服务端控制台**上。

#### ① 路径前缀的三条约束（开关若切回前缀，这些仍然成立）

- **前缀是整站的，不是只管页面。** `/api/admin/*` 也必须在前缀之内。
  只藏页面而把接口留在根路径，等于把「这里有个后台」白送给扫描器。
- **前缀之外的一切都返回同一个 404**（同上：不区分「路径不存在」与「前缀不对」）。
- **前端不许硬编码路径。** 页面里的 `{{BASE}}` 占位符由 `src/pages.js` 下发时替换；
  脚本里的 URL 一律经 `window.PFXT_BASE` 拼。写死 `'/api/...'` 的后果是资源全 404，
  而且只在带前缀的地址下才出现。前缀关掉时 `{{BASE}}` 注入空串，前端无需改动。

**两种方式都只是「不被发现」，不是权限控制。** 真正的鉴权仍是管理员口令 +
内存会话与评委登录码；暗号泄露的后果是退回原状（一个挂在公网上的后台），不是失守。

**无 HTTPS 的影响已核查，是安全的**：`public/admin.js` 已用 `window.isSecureContext`
判断过 `navigator.clipboard`，纯 HTTP 下自动走 `execCommand` 回退；
客户端代码没有使用 `crypto.randomUUID`（该 API 同样要求安全上下文）。
服务端的 `crypto.randomUUID` 在 Node 里不受影响。

---

## 5. 数据模型

### 5.1 表结构

```sql
contestant (id, seq, name, project, intro)                    -- 演讲者名单
dimension  (id, seq, name, weight, detail)                    -- 评分维度

-- 登录码表：只回答「这个码是否存在 / 是否作废」，绝不指向选票
invite (code PK, created_at, revoked)

-- 场次：一位演讲者一个时段。同一演讲者可以有多场（重开场景，见 §5.4）
round (id PK, contestant_id, seq, status, opened_at, closed_at)
    + 部分唯一索引 round_single_live ON round(status) WHERE status='live'

-- 提交记录：与 ballot 无任何关联键。这是「谁交了第几场」的唯一来源。
round_submission (round_id, code, submitted_at, PK(round_id, code)) WITHOUT ROWID

-- 选票表：⚠️ 只允许有 ballot_id 这一列
ballot (ballot_id PK) WITHOUT ROWID

-- 评分行：用 round_id 归属场次
score (ballot_id, round_id, dimension_id, value, PK(ballot_id, round_id, dimension_id)) WITHOUT ROWID

setting (key PK, value)
```

**`round_single_live` 是关键**：部分唯一索引让「同时最多只能有一个进行中的场次」
由**数据库**保证，而不是靠 `advance()` 自觉 —— 管理员连点两次、或以后加了别的入口，
都不可能造出两个 live 场次。

**相对旧库的变化**：`invite` 的 `status`/`opened_at`/`submitted_at` 三列删除；
`score.contestant_id` 改为 `round_id`（重开场景下同一演讲者有多场，用 contestant_id 分不开）；
新增 `round` 与 `round_submission`。

**老库拒绝启动**：`PRAGMA user_version` 不是 2 时直接抛错并给出处理步骤。
不做自动迁移 —— 旧的「一次性提交」选票无法映射到任何场次，
静默转换只会让人误以为数据还在。

### 5.2 ⚠️ 评委身份：选票记录登录码（2026-09-18 变更）

**业务方要求结果页能列出「每个登录码 × 每位演讲者 × 每个维度」的评分，
因此 `ballot` 带 `code` 列。选票与登录码之间可以 join，管理员能还原
「谁给谁打了多少分」。这是刻意的产品决定。**

在此之前的版本用 8 条断言保证「两表无任何关联键」，那套设计留在 git 提交 `e51d91a`
与本文档 §14 的变更记录里备查。**已经投出去的旧票无法追溯** —— 库里没有码，
永久不可归属。

`src/db.js` 的 `assertSchemaIntegrity()` 每次启动逐条断言，保证**结构没被改坏**
以及**数据最小化**：

1. `ballot` 列集合**严格等于** `{ballot_id, code}`
2. `score` 列集合**严格等于** `{ballot_id, round_id, dimension_id, value}`
3. `round_submission` 列集合**严格等于** `{round_id, code, submitted_at}`
4. 三张表必须 `WITHOUT ROWID`
5. `ballot` 禁止出现 IP / User-Agent / 时间戳等**请求元数据**列 ——
   `code` 是业务要的，但那些没有理由落库
6. 提交接口只允许把 `code` 写进 `ballot`，禁止写 IP / UA / 时间
7. 库里禁止出现 view / trigger（能绕过上面的列集合检查）

列名检查是**集合精确相等**（不是「不多于」），违反即拒绝启动。

### 5.3 评委身份变更带来的两点影响

**① 匿名承诺已撤回，必须通知评委。**
Excel 的「计分说明」页以书面形式写明：本次打分**不是匿名的**，每张选票都能对应到
发放的登录码。若评委此前被告知匿名，需要更正。

**② 管理员能看到逐条明细，比赛期间不要投屏。**
「明细」页会把「谁给谁打了多少分」整张列出来。一旦评委知道自己的打分会被逐条查看，
后续场次的评分就可能不再独立 —— 页面上有一句常驻提示，但真正的约束是纪律。

仍然**不记录**的：评委的 IP、设备（User-Agent）、提交时刻。所以「谁投的」可查，
「从哪里投的」「用什么手机投的」查不到。

票数 ≤ 2 的场次仍加「薄数据」标注 —— 那影响的是**成绩可信度**，与身份无关。

### 5.4 重开一场的语义

- 重开**不是**把旧场次的 `status` 改回 `live` —— 那会让已经交过的评委被允许再交一张，
  票数凭空翻倍，而且新旧票在库里分不开（同一 `round_id`）。
- 重开 = **为同一位演讲者新建一场**（`seq` 更大）。计分时只取该演讲者 `seq` 最大的那一场，
  旧场进 `superseded`，在后台置灰展示、进导出备注、**不计入排名**。

---

## 6. 计分规则（核心算法）

⚠️ **2026-09-18 改口径**：计分改为**完全对齐活动方《Dify工作流竞赛作品评选方案》**。
改动的原因是发现系统算法与正式方案对不上：

| | 《评选方案》 | 系统改前 |
|---|---|---|
| 去分单位 | 每位评委的**加权总分**去掉 1 高 1 低 | 每个「场次×维度」格子各自去一高一低 |
| 最终得分 | 剩余求平均，**四舍五入 2 位小数** | 未取整的精确值 |
| 同分 | 4 条自动顺位 + 1 次无记名投票 | 直接并列，无顺位 |

**两者结果不同**：多维度下「各维度各自去分」≠「按评委总分去分」。
一个「某维度给高、另一维度给低」的评委，在旧口径下被两个维度分别当成极值处理，
在新口径下他只是**一个总分**。方案第四顺位写的是「全部评委（**不去最高、最低**）
的加权总分」，只有去分发生在评委总分层面这句话才讲得通 —— 反过来印证了主口径。

### 6.1 有效票

一张选票 = 一位评委对**一位演讲者**的一次提交（只属于一场）。
必须**覆盖全部当前维度**才算有效票：按总分去分要求每张票的总分是完整的，
残缺票的总分是残的，拿去和完整票一起排序再「去掉最高最低」，
等于把去分保护用在一个错误的数上。

残缺票（`incomplete`，单位「张」）与指向已删场次/维度的残留行
（`orphans`，单位「行」）都**不计分**，但**分开计数** —— 成因不同，混在一起会查错方向。

### 6.2 按评委加权总分去分（自适应票数）

`T_b = Σ_d w_d × s_bd`（整数，权重合计 100、每维 1–5 → `T_b ∈ [100,500]`）。
`T` 升序排序后：

| 有效票数 | 处理 | 标记 |
|---|---|---|
| ≥ 3 | 去掉首尾各一个（各一个，不是去所有极值），其余取平均 | 正常 |
| 2 | 两个都保留（去分会把票去光，反而丢掉全部信息） | — |
| 1 | 保留这个总分 | `single: true`，页面标「仅 1 票」 |

⚠️ `single` 的判据是**实际收到的票数**，**不是**去分后剩下的票数。
3 票去一高一低之后也只剩 1 票，用 `keptCount` 判断会把 3 票的场次全标成「仅 1 票」，
而票数明明有 3 张。那种情况另用 `trimmedToOne` 标记，页面显示「去分后剩 1 票」。

| 0 | 该场不计分 | 整行 `blank`，`total = null`，名次 `null` |

**每一场都必须产出结果**，票数再少也不抛错、不阻塞导出。

> 恰好 3 位评委时，去分后只剩中间那一位的总分 —— 等于由他一个人决定该场。
> 不是 bug，但结果页会标出来。

### 6.3 同分顺位（《评选方案》同分细则）

最终得分**取整后**相同，依次比较，高者优先。全部用**精确整数**：

| 顺位 | 键（缩放后） | 方案原文 |
|---|---|---|
| ① | `rawSum_w30 × (Lraw/n)` | 现有业务流程契合度（30%）原始平均分 |
| ② | `rawSum_w25 × (Lraw/n)` | 长期重复使用价值（25%）原始平均分 |
| ③ | `rawSum_w15 × (Lraw/n)` | 完整性与可用性（15%）原始平均分 |
| ④ | `Σ_d w_d × rawSum_d × (Lraw/n)` | 全部评委（不去最高、最低）的加权总分 |
| ⑤ | — | 评委组针对同分项目无记名投票 —— **系统不代劳**，只标「待投票」 |

- `Lraw = LCM(所有场次的有效票数 n)`，`rawSum_d` 是**不去分**的原始分之和。
  用 `Lraw/n` 缩放是为了比**平均**而不是总和 —— n 相同时两者等价，
  n 不同时比总和会偏袒票多的场次。
- 顺位 ①②③ 按**权重**认维度，不按名字、不按 `seq`：名字可能被改、`seq` 可能被调，
  权重在开赛后是冻结的，且方案原文自己就是用权重标注的。找不到对应权重就跳过该顺位。
- ⚠️ 顺位 ③ 是 **15%** 的「完整性与可用性」，**刻意跳过** 20% 的「效率提升价值」。
  这是方案原文的顺序，**不是笔误**，不要「顺手修正」成按权重降序。
- 四条顺位全同 → 真并列，下一名跳号，并标 `needsVote`。

**为什么排名主键是取整后的分数**：方案规定「四舍五入保留 2 位小数，作为最终得分」，
并据此排名。所以两条精确值不同、但都取整到 4.63 的场次现在算**同分**，
由顺位分高下 —— 同分细则正是为取整而存在的。
`total` 直接取 `score100 / 100`，与排名同源，页面上不会出现「显示相同却名次不同还看不出为什么」的错位。

**跨等平分**：若一个「四条顺位全同」的并列组横跨奖级边界，该组标「X / Y 待投票」，
**不替评委组指定奖级** —— 照位置各发一个奖等于替他们做了决定，还会让固定名额落空。

### 6.4 ⚠️ 全部比较仍必须用整数

分母不同之后 `U` 变成有理数，禁止退回浮点比较。把分母统一到
`L = LCM(所有出现过的 keptCount)`：

```
uScaled  = sumKept × (L / keptCount)        ← 整数
score100 = intDiv(2 × uScaled + L, 2 × L)   ← 排名主键 =「最终得分 × 100」
total    = score100 / 100                   ← 展示用，1.00–5.00
totalExact = uScaled / (WEIGHT_TOTAL × L)   ← 未经取整的精确值，仅审计用

intDiv(a, b) = (a - (a % b)) / b            ← 精确整除
```

⚠️ **不能写 `Math.floor(a / b)` 或 `Math.round(x * 100)`**：`a` 最大到 ~4.7e12，
浮点除法在整数边界上可能落成 `x.9999999`，floor 之后少 1，名次就悄悄错一位；
`Math.round(1.005 * 100)` 返回 `100` 而不是 `101`。取余是精确的，先减余数再除。

**量级证明**：评委人数上限 `MAX_JUDGES = 20`，`keptCount ∈ [1,20]`，
`LCM(1..20) = 232,792,560`。`sumKept ≤ 500×20 = 10^4` → `uScaled ≤ 2.33e12`，
`2·uScaled + L ≤ 4.7e12`；顺位键 ≤ `10^4 × 2.33e8 = 2.33e12`。
全部远小于 `Number.MAX_SAFE_INTEGER`（`9.007e15`）。
实现里对 `L` 与 `Lraw` 都设了 `LCM_LIMIT` 硬闸，并断言每个 `u` 是安全整数 ——
超出设计前提时**宁可拒绝出结果，也不要算出悄悄丢精度的排名**。

### 6.5 排名与奖级

```
排序：blank 最后 → score100 ↓ → ① ↓ → ② ↓ → ③ ↓ → ④ ↓ → seq ↑（仅稳定显示顺序）
名次：五元组完全相同 → 并列，下一名跳号
```

**奖级按名次表里的位置分配**：位置 1–2 一等奖、3–5 二等奖、其余三等奖。
用位置而不是名次 —— 方案给的是名额数，并列跳号后按名次发会多发。
参赛作品不足 6 件时二等奖或三等奖会空出：**名额是上限不是保底**。

### 6.6 函数签名

```js
computeResults({ rounds, dimensions, ballots })
// → { rounds: [{ roundId, contestantId, seq, name, project,
//                n, thin, blank, margins, dimStats, trim,
//                u, total, totalExact, score100, keys,
//                rank, tied, needsVote, tieBreakLevel, award, awardPending }],
//     lcm, lcmRaw, orphans, incomplete, insufficient, tieBreak }

pickEffectiveRounds(rounds)   // 同一 contestantId 只保留 seq 最大的一场
awardForPosition(position)    // 位置 → 奖级
intDiv(a, b) / roundHalfUp(num, den)   // 精确整数运算
lcm(values)
```

- `margins[d.id]` 是**原始平均分**（不去分），与同分顺位、明细页「均分」行同口径
- `trim` 是**场次级**的去分信息（`keptCount / trimmed / single / trimmedToOne / removedLow / removedHigh`）
- `dimStats[d.id]` 是**维度级**的原始统计（`counts` 直方图、`rawSum`、`rawAvg`）

---

## 7. 评委端

### 7.1 登录：统一入口 + 手输短码

- 登录码由管理员在后台**批量预先生成**（`POST /api/admin/invites`），一对一发给评委
- 统一入口 `/v` 是**登录码输入页**，不自动发码（旧版的 `/api/v/claim` 已删除）
- 输入规范化：去空白与连字符、统一大写。字符集不含 `I/O/0/1`，对手输友好
- 登录成功后码存 `localStorage` 并带进 URL（`/v/<code>`）

**⚠️ 短码因此从「随机标识」变成了「持有即可用的凭据」**：旧流程里猜不中可以直接领一个新的，
现在猜中就能冒名投票。所以 `/api/v/login` 单独限流。

**限流额度刻意不是 10 次**：现场 20 个评委多半连同一个公司 WiFi，出口 NAT 后是
**同一个公网 IP**。按 10 次/10 分钟算，几个人手滑打错就能把全场锁在门外。
真正的防线是短码空间 `32^8 ≈ 1.1e12`（暴力枚举不可行），
限流只是挡住脚本扫描的减速带，所以取 **60 次 / 10 分钟 / IP** 并记录失败日志。

### 7.2 状态机

| 状态 | 触发条件 | 界面 |
|---|---|---|
| `loading` | 刚打开 | 转圈 |
| `blocked` | 码无效 / 已作废 / 投票已结束 / 连不上 | 阻断页（无效时可点「重新输入登录码」） |
| `waiting` | 已登录，当前没有 live 场次 | 「等待主持人开始下一位」+ 轮询 |
| `score` | 有 live 场次，本场未提交 | ★ 唯一的打分屏，**只显示当前这一位** |
| `submitted` | 有 live 场次，本场已提交 | 「本场已提交，等待下一位」 |

**「看不到其他演讲者」由三层保证**：

1. **服务端**：`/api/v/:code/state` 的响应体里**只有当前这一位**，
   绝不返回 `contestants` 数组。验收用 `JSON.stringify` 断言响应体不含其他演讲者姓名。
   维度也不下发 `weight` —— 评委不需要看到权重，那只会让打分时先在心里加权一遍。
2. **前端**：没有上一位/下一位/跳转控件，也没有位置记忆。
3. **数据**：任何人的分数本来就不下发到评委端。

**提交后不回放自己的分数**：`submitted` 屏只说「已提交」，不展示打过的分。
这是对「提交后也无法查看上一个演讲者的得分信息」最保守的解读。

**换场时若上一场有没提交的草稿**，新场次顶部会出现一条提示
「上一场已经结束了，你没来得及提交，那一场记为弃权」——不能悄悄吞掉。

### 7.3 轮询，不用 WebSocket / SSE

打分页与等待页每 **3 秒** 轮询一次 `/api/v/:code/state`。

理由：企业微信内置浏览器对 `ws://` 支持不佳（官方社区明确建议用 `wss://`），
而当前部署是纯 HTTP，`wss` 无从谈起；SSE 在该环境下的支持情况查不到任何官方说法。
轮询还能天然熬过「WebView 切后台」「链接改在系统浏览器打开」这类上下文切换。
负载上，20 人 × 20 场 × 每 3 秒一次对一台 2 核机器完全不构成压力。

**上下文切换的应对**：同一个链接在企微内置浏览器和系统浏览器里是两个独立的存储域，
localStorage 不互通。所以短码**同时**存在于 URL 路径与 localStorage 中，
读取顺序是 **URL 优先 → localStorage 兜底 → 都没有才回输入页**。

**网络失败时不替换正在打分的界面**：草稿存在本机，只在提示行说明「网络不稳，正在重试」。
贸然切屏会把评委正在打的分从眼前拿走。

### 7.4 草稿

只存浏览器本地，服务端不存半成品。键名按场次隔离：

- `pfxt:code` —— 登录短码
- `pfxt:draft:<CODE>:<ROUND_ID>` —— 本场未提交的打分

换一位演讲者就是一份全新的草稿，上一场没交的草稿不会被下一场复用（那会张冠李戴）。

---

## 8. 管理端

### 8.1 场次控制（默认页）

顶部控制条：当前场次 + 「已收 X / N」+ **开始下一位** 按钮。
下方是场次记录列表（倒序）、跳到指定演讲者、登录码概况、危险操作区。

- **已收 X / N**：`N` 取自「评委人数」配置项
- **超员告警**：某场提交数 **大于** 评委人数时高亮 —— 通常意味着有人清掉浏览器缓存后
  又拿到了新的登录码，必须当场核对。这是自服务登录模型下唯一的异常信号。
- **薄数据标注**：票数 ≤ 2 的场次标出来
- **重开**：见 §5.4
- 名单跑完后「开始下一位」自动禁用

### 8.2 配置锁定

**判据是「有没有开过场」**，不是「有没有选票」—— 开了场就意味着已经有评委会看到打分页。

- 开赛后：**维度集合与权重冻结**（否则可以在场次之间偷改权重，
  追溯性改变已完成场次的分值）；前端把对应输入框禁用，别让人白填一遍再被拒
- 允许：改活动名称、姓名、项目名、简介；**允许中途加人**（临时补位是真实需求）
- 禁止：删除**已经上过场**的演讲者（那会让 `round` 关联不到人、分数变成孤儿）。
  要退出就别再给他开场次。

### 8.3 一键重置（彩排用）

`POST /api/admin/reset`，要求请求体带字面量 `{ "confirm": "RESET" }`。

清空 `round` / `round_submission` / `ballot` / `score` / **`invite`**；
**保留** `contestant` / `dimension` / `setting`。

⚠️ **2026-09-22 变更：登录码从「保留」改为「全部删除」。**
原先的理由是彩排完不用重发一遍码；但彩排期间发出的码可能已经落到非正式评委手上、
或留在测试设备里，正式场次应当只认新签发的码。现在重置后 `invite` 表为空：
「登录码」页回到空态，必须重新批量生成发放，仍在用旧码的浏览器拿到「登录码无效」。

删除顺序上 `invite` 排在 `round_submission` 之后 —— 作废**单个**码时会拒绝
「交过任一场」的码（见 §9 接口表），整表删除没有这个约束，但保持同样次序不容易踩坑。

### 8.4 结果页

⚠️ **2026-09-18 两次改版**：先删掉五个维度各自的均分列，改成以名次为中心；
同日再**撤掉奖级列和票数列**，页面上只剩排名。现场公布时维度分是噪音 —— 它淹没名次；
要看维度去「明细」页或 Excel 的「维度明细」。

- **横幅只有一句状态**：「比赛进行中（已收 N 份评分）· 名次为**暂定**」/
  「投票已结束（共 N 份评分）」。不在这里展开说明为什么不能提前公布。
  比赛没结束时必须写「暂定」—— 后面还有作品上场
- **概要行只呈现结果本身**：共几位、票数不足的场次、待投票的场次、被重开顶掉的场次。
  ⚠️ **不再重复「有效评分 N 份」** —— 份数已经在横幅里，同一屏说两遍是噪声。
  **不讲解计分规则**：规则是固定的，讲在页面上只会淹没结果；需要时看 Excel 的「计分说明」页
- 表格：**名次 / 演讲者 / 项目 / 加权总分**（四列，**没有奖级、没有票数**）
- **名次徽章放大，前三名按金 / 银 / 铜着色**（色值在 `common.css` 的
  `--gold/--silver/--bronze` 三组变量里，**亮色与暗色主题各定义一次**）。
  并列待投票的用虚线告警色徽章，与「已定名次」区分开。
  ⚠️ **这是「名次」配色，不是「奖级」配色** —— 二者边界并不重合
  （奖级是一等奖 2 名、二等奖 3 名，这里是前三名）。改这块时别按奖级去想
- **同分判定必须显示出来**：最终得分取整到 2 位小数之后同分是常态，
  两行都显示 4.63 却一前一后，不给说法现场就会以为排错了。
  被顺位分开的行在分数旁加「同分」小标，悬停显示是第几顺位、按哪个维度分的
- **奖级仍然照 §6.5 计算，只是不在结果页显示** —— Excel 的「汇总」工作表里还有。
  撤掉的是**显示**，不是算法
- **版式**：列少了，靠留白撑 —— 行高加大（td `15px 18px`）、名次列 92px、
  姓名列钉死 240px（否则表格会把富余宽度全塞给「项目」，姓名右边空出一大片）、
  总分放大到 17px。偶数行斑马纹 ⚠️ 用 `color-mix(in srgb, var(--text) 4%, var(--surface))`，
  **不要用 `--surface-2`** —— 它离 `--surface` 只差 2%，投影上看不出来。
  斑马纹刷在 `td` 上，而 `.grid tbody tr:hover` 刷在 `tr` 上，td 盖住 tr，
  所以 **hover 必须在 `td` 这一层重新声明**，否则有斑马纹的行悬停毫无反应
- **`single` / `trimmedToOne` 角标挪到「演讲者」一格**：维度列没了之后它们无处可挂，
  而且这两件事本来就是**场次级**的（收到几张票），挂在某一维度上反而不准确
- **薄数据标注**：票数 ≤ 2 的场次加角标
  - `single`（真的只收到 1 票）→「仅 1 票」
  - `trimmedToOne`（收到 3 票，去一高一低后只剩 1 票）→「去分后剩 1 票」
  - 后者在**恰好 3 位评委**时每场都会出现 —— 那不是 bug，是「去一高一低」在 3 票时
    把两头各去掉一个、结果等于中间那位说了算。评委人数正常（≥4）时不会出现
- 0 票的场次排末尾、总分显示「—」、**不阻塞导出**
- ⚠️ 前端**不得自己重算并列**：判据从 `u` 变成了五元组，前端再算一遍必然与服务端漂移。
  直接用服务端下发的 `tied` / `needsVote` / `tieBreakLevel`
  （`award` / `awardPending` 服务端仍在下发，供 Excel 用，结果页不再读）

### 8.5 评分明细页

**一张整宽的矩阵**（2026-09-18 改版，见 §14.6）：行 = 登录码，列 = 维度，末列小计。
一页一张表，吃掉整个页宽 —— 不是一人一张小卡片并排。

每位演讲者占**一段**：先一行跨列的「姓名 - 项目名」（居中，**不带序号** ——
出场顺序是「场次」页的事，这一页只认人），随后是这位演讲者的评委行。

**表头钉在顶部**：整张表只有 `.detail-wrap` 这一个滚动容器，滚轮走多远，
「哪一列是哪个维度」都还在眼前。这正是把所有人放进**同一张表**、而不是拆成
多张各自滚动的卡片的原因 —— 拆开之后表头就得每个容器钉一次，而每个容器都矮一截。

列宽用 `table-layout: fixed` 由表头那一行定死，因此**表头与表体的列宽必然对齐**；
登录码 148px、小计 108px，剩下的宽度五个维度平分。维度名允许折行，不把表撑宽。

窄到 **760px** 以下才横向滚动（再窄列宽就没法看了），那时登录码列钉在最左并
补上底色与投影 —— 宽屏下这两样都不加：钉住是白钉的，而一旦有底色，这一列就压掉了
`.grid` 的整行 hover 高亮，读长行时反而少了参照。

弃权的格子显示「—」并压暗。**「—」的含义是这位评委这一场没有提交，不是打了 0 分。**
一次都没交过的码也留在表里（名单取自 invite 表而不是选票），否则「谁弃权了」反而看不出来；
已作废的码保留并标红。登录码下方**不再挂「已评 N 位」** —— 整宽表里每个码下面都
重复一遍，属于噪音。

这张表**不参与 20 秒自动刷新**：重建 DOM 会把滚动位置冲掉，手动点刷新即可。

**被去掉的那两行要划出来**（2026-09-20 追加，见 §14.8）：一场之内被「去一高一低」
去掉的那两位评委，整行画一条浅浅的横线，「（最高分）/（最低分）」挂在**登录码右侧**。

- 标记由服务端下发（`cell.trimmedAs` ∈ `high` / `low`），**前端不自己反查** ——
  反查要拿总分去比对，前端再算一遍就是又一处会跟服务端漂移的判定
- ⚠️ **票数 ≤2 的场次不标**。此时 `trim.removedLow/High` 只是首尾本身、
  并不代表「被去掉」（见 `scoring.js` 同名注释）。必须按 `trim.trimmed` 判断，
  否则会给这些场次的首尾两行平白画上删除线
- ⚠️ 边界同分时（最低或最高不止一位）去谁**结果完全一样**（留下的多重集不变），
  但「哪一行被划掉」数值上无法唯一确定。服务端按登录码定序取一位以保证稳定，
  并给 cell 打 `trimTied`，页面把这一点放进标签的 `title`
- 横线画在 td 的 **背景**上而不是用 `text-decoration: line-through`：
  后者会把「（最高分）」这个**注解本身**一起糊掉（CSS 的 text-decoration
  无法由后代关掉）。分值压暗，但**登录码那格不压暗** —— 压暗了就看不清是谁被去掉的

### 8.6 Excel 导出

两组导出，共五张表：

**结果报表**（`/api/admin/results.xlsx`）：**汇总** / **评委明细** / **维度明细** /
**去分明细** / **计分说明**。

- **汇总**与页面一致：名次 / 奖级 / 演讲者 / 项目 / 加权总分 / 本场票数 / 同分判定。
  **不含维度分**（维度分在「维度明细」里）
- **去分明细**是评委总分口径的**审计留痕**：本场票数、去分方式、全部评委加权总分、
  去掉的最高/最低总分、去分后票数、去分后总分、最终得分。
  现场对分数有异议时要能逐场说清
- 总分一律换算到 **1.00–5.00**，与页面同口径，避免「100–500 的评委总分」与
  「1–5 的维度分」并排出现让人读错

**明细报表**（`/api/admin/detail.xlsx`）：一位演讲者一张工作表，只有逐条打分，
**不含**汇总、维度统计与计分说明 —— 明细页该导出明细。

计分说明页以书面形式写明这几件事，全部是交付文件，**不能与系统实际行为不符**：

1. **取分规则**：按每位评委的加权总分去一高一低，四舍五入保留 2 位小数
2. **同分细则**：实际生效的顺位链（写出当时命中的是哪些维度）+
   第四顺位 + 第五顺位由评委组投票
3. **奖级名额**：一等奖 2 名、二等奖 3 名、其余三等奖；不足 6 件时名额会空出
4. 选票记录登录码，本表**可以**还原「哪位评委给谁打了多少分」
5. 仍然不记录的：IP、设备、提交时刻
6. 需要如实告知评委的两点：本次打分不是匿名的；无法验证「拿码的人就是本人」
7. **活动阶段**：比赛进行中导出时，表里的名次与奖级都标注为**暂定**

---

## 9. 后端 API

⚠️ **下表所有路径都还受 §4.1 的暗号保护**（当前默认是请求头令牌，**只拦 GET/HEAD**）。
表里写的是路由文件里定义的样子；
`src/routes/*.js` 里**不拼前缀** —— 由 `src/server.js` 用
`app.use(BASE_PATH, router)` 整体挂载，前缀只在那一个地方出现。

### 评委端（`src/routes/vote.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/v` | 登录码输入页 |
| GET | `/v/:code` | 投票页（码无效也返回页面，由前端判定，好显示中文提示） |
| POST | `/api/v/login` | 校验登录码 → `{ ok, code }`。严格限流（§7.1） |
| GET | `/api/v/:code/state` | 评委端唯一数据来源。**只含当前演讲者，不含 contestants 数组** |
| POST | `/api/v/:code/submit` | `{ roundId, scores: [{dimensionId, value} × |D|] }` |

提交事务在事务内重查「这一场还是不是当前 live 场」与 phase，
既挡住并发双击，也挡住「管理员恰好在你提交时切了场」；
`round_submission` 的主键 `(round_id, code)` 是最后一道防线。

### 管理端（`src/routes/admin.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/rounds` | 场次总览 + 登录码概况 + 异常告警（取代旧的 `/progress`） |
| POST | `/api/admin/rounds/advance` | `{ contestantId? }`，不传则按 seq 取下一个没上过场的 |
| POST | `/api/admin/rounds/:id/reopen` | 为同一演讲者新建一场（§5.4） |
| POST | `/api/admin/reset` | 一键重置（§8.3） |
| GET | `/api/admin/results` `/.xlsx` | 按 §8.4 |
| GET | `/api/admin/invites` | 含 `rounds_submitted` / `last_submitted_at` |
| POST | `/api/admin/invites/:code/revoke` | 交过任一场则拒绝作废 |
| PUT | `/api/admin/config` | 含 `judgeCount`，开赛后锁见 §8.2 |
| — | `/login` `/logout` `/session` `/entry` `/qrcode.svg` `/backup` `/close` `/reopen` | 不变 |

---

## 10. 文件与实施顺序

| 文件 | 职责 |
|---|---|
| `src/db.js` | 建表、老库拒绝、**启动期匿名性断言**（§5.2 的 8 条） |
| `src/scoring.js` | 纯函数计分：自适应去分 + LCM 缩放（§6） |
| `src/rounds.js` | 场次读写：`getLiveRound` / `advance` / `reopenRound` / `resetAll`（唯一会碰 `ballot`/`score`/`invite` 的函数，见 §8.3） |
| `src/routes/vote.js` | 评委端接口 |
| `src/routes/admin.js` | 管理端接口 |
| `src/net.js` | **口令暗号的唯一真源**：路径前缀（`DEFAULT_BASE_PATH` / `PFXT_BASE_PATH`）+ 请求头令牌（`DEFAULT_API_TOKEN` / `PFXT_TOKEN`）+ `PFXT_PUBLIC_URL` 支持 |
| `src/pages.js` | 下发页面时替换 `{{BASE}}` 占位符（`server.js` 与 `routes/vote.js` 共用） |
| `src/seed.js` | 默认名单 / 维度 / `judge_count` / 管理员口令 |
| `public/entry.html` | 登录码输入页 |
| `public/vote.{html,js}` | 评委端状态机（§7.2） |
| `public/admin.{html,js}` | 管理后台，默认落在「场次」页 |
| `test/scoring.test.js` | 计分单元测试 |
| `test/acceptance.js` | 端到端验收（§11） |

---

## 11. 验收清单

### 单元（`npm test`）

- 精确整数：`intDiv` / `roundHalfUp`，并钉住「`Math.round(1.005*100)` 会给出 100」
- 按评委加权总分去分：`n=1/2/3/5/6` 的 `keptCount` 与 `trim` 字面量
- **跨场次混合**：11 票（keptCount 9）与 2 票（keptCount 2）同场，`L = LCM(9,2) = 18`
- **★ 去分单位是评委总分**：4 位评委、权重 50/50 的用例里新口径 3.00、旧口径 2.50，
  把两者**不相等**这件事钉死（旧的「退化等价」用例在数学上不成立，已替换）
- **★ 取整后同分**：两场 `u` 不同（40000 / 39900）但都取整到 1.05 → 判为同分，
  由第四顺位分出 `[1, 2]`
- **★ 同分顺位 ①②③④** 各一条；顺位链是 `30 / 25 / 15`，
  **刻意不含 20% 的「效率提升价值」**
- **★ 四顺位全同** → 并列 + `needsVote`；加一场低分的验证跳号 `[1, 1, 3]`
- **★ 奖级**按位置分配；并列组跨奖级边界 → `awardPending`
- **溢出上界**：keptCount 覆盖 1..20 时 `L = Lraw = 232,792,560`，`u`/`score100`/顺位键仍是安全整数
- 空白场次的顺位键**不得出现 `NaN`/`Infinity`**（否则整个结果页 500）
- 残缺票不计分、单独计入 `incomplete` 且**不混进 `orphans`**
- 越界拒绝、弃权不抛错、空白场次、孤儿行、重开取最新场

### 端到端（`PFXT_PASSWORD=<口令> npm run acceptance`）

> 会写库并**清空演练数据**，不要在正式数据上跑。

- **结构检查**（直读 db 文件）：三张表的列集合精确相等、`WITHOUT ROWID`、
  `ballot` 除 `ballot_id`/`code` 外没有别的列、不记录 IP/UA/时间戳、无 view/trigger、
  `ballot` 行数 == `round_submission` 行数
- **★ 归属正确性**：每张选票都能归属到一个登录码、归属结果与提交记录一一对应、
  同一个码在同一场不会有多张票
- **★ 评委明细**：结果接口返回每位评委的逐维度打分；覆盖所有签发过的码（含一次没交的）；
  分数落在 1–5；维度数与配置一致；**该场小计与手算 `Σ(权重×分)÷100` 一致**
- **★ 同名会话 cookie 并存**：新 token 与一份「旧版本残留在 `Path=/` 上的同名 cookie」
  同时送来时必须仍然认已登录（**任意一份有效即算已登录**，不看顺序）；
  只有失效的那一份时仍须 401。cookie **不区分端口**，所以改前缀后
  浏览器里必然同时留着这两份 —— 早期「后者覆盖前者」的读法会导致
  「登录返回 200 却立刻被踢回登录页」
- **★ 路径前缀把门**（二·五节，仅在前缀开着时跑；关着时该节打印一行跳过）：
  根路径下的旧地址（`/` `/admin` `/v` `/api/admin/*` `/api/v/*` `/admin.js`）
  **全部 404**；且未带前缀的 404 与随机路径的 404 **响应体逐字相同**
  （措辞不同等于自曝前缀）；页面里的资源路径都带前缀、`window.PFXT_BASE` 注入正确
- **★ `/state` 响应体不含其他演讲者姓名**、不返回 `contestants`、不下发权重
- 登录规范化（小写/空格/连字符）、错误码 404、限流最终触发
- 场次主流程：开场 → 提交 → 重复提交 409 → 推进 → 旧 `roundId` 409 `round_closed`
- 弃权：3 个码只交 2 个就推进 → 该场 `n=2`、`thin`、计分不抛错
- **★ 计分口径**：`score100` 是整数且 `total × 100 == score100`；
  每个非空场次都有 `rank` 与 `award`；`incomplete == 0`；`Lraw` 是整数
- **★ 3 票的场次**：`single === false`、`trimmedToOne === true`、`trimmed.keptCount === 1`
  （判据必须是「实际收到几票」，不能是「去分后剩几票」—— §14.4 的回归）
- 同码交两场 → `COUNT(DISTINCT ballot_id) = 2`
- 封盘 / 重新开放；重开一场；一键重置后**登录码仍然有效**
- 连点 advance 不产生两个 live 场（数据库层已由唯一索引保证）

### 真机彩排（**不可省**）

1. 两台手机（iOS + Android）**在企业微信里**点链接，确认打开的是哪个浏览器
2. 走完「输码 → 打分 → 提交 → 等待 → 主持人切换 → 新场自动出现」
3. 手机息屏 5 分钟再唤醒，确认轮询恢复、登录态还在
4. 确认企微与系统浏览器两个上下文里短码能否互认（预期：不能，走 URL 兜底）
5. 跑一次一键重置

---

## 12. 风险与开放项

| # | 风险 | 处理 |
|---|---|---|
| R1 | **腾讯云拦截内地 IP:端口** | §3 的 Day-0 验证；不通立即换香港节点。**最高优先级** |
| R2 | **企微对未备案链接弹风险提示** | Day-0 就要在企微里实测点击 |
| R3 | **企微内置浏览器 vs 系统浏览器存储域不互通** | 短码 URL 优先 + localStorage 兜底（§7.3） |
| R4 | **企微有概率清掉 localStorage** | 同上，URL 兜底让这件事变成「重输一次短码」而不是「登录丢失」 |
| R5 | **评委知道自己的打分会被逐条查看 → 后续场次评分不再独立** | 「明细」页常驻提示不许投屏（§5.3 ②）。这是纪律问题，技术挡不住 |
| R5b | **若此前已告知评委「匿名」** | 必须更正。Excel 计分说明页已写明实际行为（§8.6） |
| R6 | **短码成为凭据后可被猜** | 限流 + 失败日志（§7.1）；主防线是 32^8 的密钥空间 |
| R7 | **清缓存换码重复投票** | 结构上无法识别（没有身份）。后台「超员」告警把异常摆到眼前 |
| R8 | **管理员_session 是进程内 Map** | 单机 + pm2 单实例下没问题。**若以后迁 Serverless 会随机掉线** |
| R9 | **`PFXT_PUBLIC_URL` 配错 → 二维码指向内网 IP** | 启动横幅打印实际生效的公网地址 |
| R9b | **口令暗号泄露 → 退回「后台裸挂在公网上」** | 暗号不是权限控制（§4.1）。真正的门是管理员口令 + 5 分钟 10 次的登录限流。暗号只能防扫描器，防不住被人转发链接 |
| R9c | **网关没代加 `token` 头 → 页面能开但一直转圈** | 请求头令牌只拦 GET/HEAD，而浏览器发不出自定义头，**页面自己的轮询/列表 GET 全靠网关注入**（§4.1）。现场第一个要查的就是这条；服务端控制台会打印 `[guard] 拒绝 …` |
| R9d | **误把守卫扩到所有方法 → 网关只给 GET 加头时提交被自己挡死** | 刻意只拦 GET/HEAD，别顺手改宽（§4.1） |
| R10 | **管理员误点「开始下一位」** | 用「重开这一场」补救（§5.4）；彩排时验证这条路径 |

---

## 13. 沿用不变的部分

- **管理员口令**：哈希 + 明文双存、每次启动打印、`npm run reset-password` 命令。
  哈希不可逆，没有明文就没法「启动即见口令」，这是刻意的取舍。
  老库（只有哈希、没有明文）启动时自动重新生成一个并**显式告警「旧口令从此刻起失效」**。
- **启动期结构断言**：仍然逐条检查、违反即拒绝启动，但守的东西从「保证匿名」
  变成了「保证结构没被改坏 + 数据最小化」（§5.2）
- 草稿只存浏览器本地，服务端不存半成品
- 提交即终局（现在是「每场终局」）
- 平局并列、下一名跳号
- 管理员随时可见分数（靠自觉不泄露）
- 零构建步骤、原生 HTML/CSS/JS；依赖仍只有三个：express、better-sqlite3、qrcode-generator

---

## 14. 2026-09-18 实施记录（与初版方案的偏差）

| 项 | 方案 | 实施 | 原因 |
|---|---|---|---|
| 登录限流 | 10 次 / 10 分钟 / IP | **60 次 / 10 分钟 / IP** | 现场评委共用公司 WiFi 出口 NAT，同一个公网 IP。10 次会被几个手误打光，把全场锁在门外 |
| 单 live 场次 | 靠 `advance()` 判断 | **数据库部分唯一索引** `round_single_live` | 把不变量交给数据库，管理员连点或将来加新入口都造不出两个 live 场 |
| 计分说明页 | 只写匿名说明 | 增加**「必须如实告知的边界」**两条 | §5.3 的固有性质必须书面留痕，否则导出文件会声称一个系统给不出的承诺 |
| 老库处理 | 「拒绝启动」 | 明确到 `PRAGMA user_version`，并给出可执行的恢复步骤 | 光说「拒绝」没用，得告诉人怎么办 |

**验证情况**：单元测试 14/14 通过；端到端验收 91/91 通过；
管理端与评委端已在真实浏览器（Chromium，手机宽度 420px）跑通完整流程，
包括「管理员在评委打分途中切场 → 评委页 3 秒内自动切换并显示弃权提示」。

**尚未完成**：§3 的 Day-0 服务器实测（需要先购买腾讯云实例）。
这是唯一的阻断项，**必须在写任何部署配置之前完成**。

### 14.2 选票改为可归属（2026-09-18 当日追加）

业务方要求结果页列出「每个登录码对每位演讲者每个维度的评分」，因此**撤回匿名**：

| 项 | 改前 | 改后 |
|---|---|---|
| `ballot` 表 | 只有 `ballot_id` 一列 | 增加 `code` 列，选票可归属到登录码 |
| 启动断言 | 8 条，保证「两表无任何关联键」 | 7 条，改为保证「结构一致 + 数据最小化」（§5.2） |
| 断言函数名 | `assertAnonymousSchema` | `assertSchemaIntegrity`（名字不能再骗人） |
| `SCHEMA_VERSION` | 2 | 3（老库拒绝启动，见下） |
| 结果页 | 只有排名 | 新增「明细」页（§8.5） |
| Excel | 三张表 | 四张表，新增「评委明细」（§8.6） |
| 对外表述 | 「谁打了什么分」查不到 | **如实说明可以查到**，并要求更正此前的匿名说法 |

**老库无法迁移**：版本 2 及更早的库里，选票没有 `code` 列，**补不上**。
已经投出去的票永久不可归属。启动时会明确告知这一点，而不是静默转换。

**仍然保留的数据最小化**：选票里除 `code` 外不写任何请求元数据 ——
不记录 IP、设备（User-Agent）、提交时刻。启动断言会拦下试图添加这类列的手工改动
（实测：加 `client_ip` 列会被拒绝启动）。

**验证**：单元测试 14/14；端到端验收 **101/101**（新增 10 项，覆盖归属正确性与明细接口）；
明细页已在真实浏览器验证，含横向滚动时登录码列冻结、弃权格显示「—」、去分后均分末行；
Excel 四张表已解包核对内容与文案。

### 14.3 界面措辞改「场」为「位」，明细报表改为一演讲者一工作表

**① 序数一律说「第 N 位」，不说「第 N 场」。**
界面上的序数（`第 3 位 · 姜添浩`、`共 3 位`、`第 2 位票数不超过 2 张`）
与计数（`已评 N 位`、`已收 3 / 3 位评委`）统一用「位」。

⚠️ **本文档其余部分仍用「场」**，因为那是**数据模型里的概念名**
（`round` 表、场次、重开一场、`advance()`），说的是「一个评分时段」这件事本身，
与界面上的「第几位演讲者」是两个层次。看到两套词不要以为是笔误。

**② 明细报表：一位演讲者一张工作表。**
原本是一张大矩阵（所有演讲者横向铺开），演讲者一多就要横向滚动几十列。
改成**每人一张表**：表内每位评委一行、每个维度一列、末列小计、末行均分。
工作表名为 `序号. 姓名`。

工作表名必须清洗：Excel 硬限制 ≤31 字符、不能含 `: \ / ? * [ ]`、不能重名、不能为空，
而演讲者姓名是用户可编辑的 —— 不清洗的话导出的文件 Excel 会拒绝打开。

结果报表（`/results.xlsx`）**不变**，仍是四张表、仍是那张完整矩阵 ——
它要的是「一份文件看全部」，明细报表要的是「一位一位地看」，两者用途不同。

**验证**：单元测试 14/14；端到端验收 **114/114**（新增 4 项，把工作表命名规则钉死：
表数等于演讲者数、命名格式、≤31 字符、无禁用字符、互不重复）。

### 14.4 修复「仅 1 票」误标（2026-09-18 当日追加）

**现象**：3 位评委都给某位演讲者打了分，明细页也显示三条记录，但结果页每个维度的
单元格都标着「仅 1 票」。

**根因**：`details[].single` 的判据写成了 `keptCount === 1`（去分后**剩下**几票），
而不是 `raw.length === 1`（实际**收到**几票）。3 位评委去一高一低之后正好剩 1 票，
于是 3 票的场次全被标成「仅 1 票」——**界面在说谎**。

同一处错误在 Excel 的「去分方式」列也有：它把 3 票的单元格写成「仅 1 票，不去分」，
而实际上去了分。

**修复**：
- `single` 改为 `raw.length === 1`
- 新增 `trimmedToOne`（`raw.length > 1 && keptCount === 1`），页面显示「去分后剩 1 票」，
  与「仅 1 票」分开 —— 两种情况成因不同，标签不能混
- Excel 的「去分方式」判据从 `keptCount` 改为 `trimmed`（有没有去分），
  这才是决定「去分方式」的那个变量

**为什么之前的测试没抓到**：单测覆盖了 n=1/2/5/6，断言了 `keptCount` 和 `trimmed`，
唯独**没有断言 3 票时的 `single`** —— 而 3 票正是唯一会踩中的取值。
现已补一条穷举 1–6 票的回归用例，把四个标记逐一体检。

**验证**：单元测试 **15/15**；端到端验收 **118/118**（新增 4 项，直接断言
「3 位评委都交了的场次 n=3 且 single 必须为 false」）；已按现象原样复现并确认修复。

> ⚠️ 当日晚些时候 §6 改了计分口径（改为按评委加权总分去分），
> `details[].single` 随之搬到**场次级**的 `trim.single` ——
> 判据本身（按实际收到的票数）没变，只是从「每维度一份」变成「每场一份」。
> 上面这条修复的**语义仍然是有效的**，回归用例也一并搬了过去。

### 14.5 计分口径对齐《评选方案》（2026-09-18 当日追加）

**背景**：活动方《Dify工作流竞赛作品评选方案》给出了正式的取分规则与同分细则，
系统当时的实现与它对不上（详见 §6 开头的对照表）。

**改动**：
- 去分单位从「每个场次×维度格子」改为「每位评委的加权总分」
- 排名主键从「未取整的整数 `u`」改为「四舍五入 2 位小数后的 `score100`」
- 新增《评选方案》的四条同分顺位；四条全同才并列，并标「待评委组投票」
- 新增奖级（一等奖 2 名 / 二等奖 3 名 / 其余三等奖），按名次表位置分配
- 结果页删掉五列维度均分，改成以名次 + 奖级为中心；Excel 增「去分明细」工作表

**代价与取舍**：
- **同一批数据算出的分数会变** —— 这是改动的目的，但彩排时若有人记下过旧分数，需要说明
- **同分变多**：最终得分只保留 2 位小数，比原来的「精确整数比」粗，
  所以结果页**必须**把「按第几顺位排定」显示出来，否则观众会以为排错了
- 原「退化等价」用例（多维度下新旧公式相等）**在数学上不成立**，只是一组
  特意构造的巧合数据（两个维度恰好去掉了同两位评委），已替换为
  「去分的单位是评委总分」的差异用例 —— 那条用例才是改口径的证据

### 14.6 明细页改为「一张整宽的矩阵」（2026-09-18 当日追加）

**背景**：明细页原本是一张把所有演讲者**横向铺开**的大矩阵 —— 行 = 登录码，
列 = 场次 × 维度。这跟 §14.3 ② 修掉的那个 Excel 毛病是同一个：演讲者一多，
列数按「演讲者数 × (维度数 + 1)」增长，看「谁给谁打了多少分」得来回甩鼠标。

> 中途试过「一位演讲者一张卡片、宽屏并排两张」。**废弃**，原因见下面的取舍第 1 条 ——
> 卡片并排等于把一页切成两个滚动容器，表头得每个容器钉一次，而每个容器都矮一截。
> 最终改成**一页一张表**。

**改动**：
- 页面上**一张表吃掉整个页宽**。每位演讲者占一段：一行跨列居中的「姓名 - 项目名」，
  随后是这位演讲者的评委行（行 = 登录码，列 = 维度，末列小计）
- **表头 `sticky top`**：整张表只有 `.detail-wrap` 一个滚动容器，滚轮走多远，
  「哪一列是哪个维度」都在眼前。**只有表头钉住**，姓名行跟着滚
- 登录码列下方的**「已评 N 位」删除**：整宽表里每个码下面都重复一遍，是噪音
- **「原始均分」行删除**：一行一位评委已经够看，均分那行在整宽表里反而把每段拉长
  （⚠️ 这是**活动方要求删的**，与 §8.5 早先描述的「基准行」相反；
  Excel 明细报表里**仍然有**这一行，两边口径自此不一致 —— 需要时以 Excel 为准）
- `table-layout: fixed` 让列宽由表头那一行定死，**表头与表体的列宽必然对齐**

**代价与取舍**：
- **不能并排两张表**。并排 = 两个滚动容器 = 表头要钉两次，且每个容器只剩半个屏高。
  「一张表 + 一个钉住的表头」比「两张表 + 两个矮滚动区」好读，所以选前者
- 宽屏下维度列拿到 176px，9 个字的维度名一行放得下，**不再折行**；
  窄到 760px 以下才横向滚动，那时登录码列才加底色与投影钉在最左。
  宽屏下**刻意不加**：钉住是白钉的，而一旦给这一列加底色，它就压掉了 `.grid` 的
  整行 hover 高亮 —— 读长行时少了一条参照线
- 姓名行**不钉住**：一屏能看到好几段，段首行本身就在视野里；再钉一行会挤掉表体高度

**验证**：单元测试 23/23（未动 `src/`，跑一遍确认无回归）。真实浏览器实测
（1280×900 与 520×800 两档）：
- **表头列宽 === 表体列宽**（`[148,176,176,176,176,176,108]` 两档逐列相等），
  即图片里那种「上下错位」不会出现
- 表宽 1138px 顶满 1140px 容器，**一页只放得下一张表**
- 滚动 900px 后表头钉住偏移 **1px**（即纹丝不动），且 `已评 N 位` 元素数为 **0**
- 窄屏（520px）：表内横向可滚 270px，滚 200px 后登录码列钉住偏移 **1px** 且底色不透明
  （数字不会从码底下穿过），整页横向溢出 **0**
- 整行 hover 高亮铺满全行（含登录码列与小计列，均为 transparent 底色）

### 14.7 结果页撤掉奖级与票数，只留排名（2026-09-18 当日追加）

**背景**：业务方要求结果页**只保留排名**，不再出现奖级和票数。列从六列减到四列，
内容偏少，因此同时把版式做厚一点撑住版面。

**改动**（`public/admin.js` / `public/admin.css` / `public/common.css`，未动 `src/`）：
- 表头与数据行删掉**奖级**和**票数**两列，只剩
  **名次 / 演讲者 / 项目 / 加权总分**
- 横幅文案「名次与奖级为暂定」→「名次为暂定」
- 概要行删掉**「有效评分 N 份」**：份数已经在横幅里说过一次了
- `tierClass(r)`（奖级 → 配色）改为 `medalClass(r)`（**名次** → 配色，前三名金/银/铜）。
  ⚠️ 这是语义变更不是改名：奖级边界（一等奖 2 名、二等奖 3 名）与「前三名」不重合，
  照名次上色**不能**当成奖级。`common.css` 里那三组变量的注释同步改了
- 删掉随之失效的 `.award` / `.award.is-*` / `.award-pending` / `.col-award` 规则
- **版式加厚**：td `padding: 15px 18px`、th `13px 18px`（原 `13.5px/5px 10px`）、
  名次列 92px、名次徽章 40px（原 32px）、总分 17px（原 14.5px）、
  姓名 15.5px 加粗、项目用 `--text-dim`
- 姓名列**钉死 240px** + 偶数行斑马纹（详见 §8.4 的版式一条，含 hover 那条坑）

**保留没动的**：
- **奖级仍然照 §6.5 计算**，Excel「汇总」「计分说明」两页里也还有 ——
  撤掉的只是结果页的**显示**
- `single` / `trimmedToOne` / `薄数据` 角标仍在「演讲者」一格。
  它们是**数据可靠性告警**，不是票数列 —— 但文案里含「票」「1 票」字样，
  若要求连这些也不出现，说一声即可去掉

**验证**：单元测试 23/23（未动 `src/`，跑一遍确认无回归）。真实浏览器实测
（1280×900，亮色与暗色各一遍，Playwright 驱动真实服务）：
- 表头四列 `名次 / 演讲者 / 项目 / 加权总分`，**奖级与票数列元素数为 0**
- 横幅为「比赛进行中（已收 7 份评分）· 名次为暂定」，概要行为「共 3 位 · ⚠️ …」
- 前三名徽章分别取到 `is-gold` / `is-silver` / `is-bronze`，第 1 名金色、
  第 3 名铜色在亮暗两种主题下都渲染正常
- 斑马纹在投影亮度下**可见**（改用 `color-mix` 前用 `--surface-2` 时不可见），
  且**悬停高亮仍能压过斑马纹**
- 姓名列 240px 生效后，「项目」列左边界由 ~527px 提前到 ~420px，姓名右侧空档消失

### 14.8 明细页划出去掉的最高/最低分（2026-09-20 追加）

**背景**：业务方要求明细页指出**哪两位评委的加权总分被去掉了**，把整行划掉并在
登录码右侧标明是最高还是最低。

**改动**：
- `src/routes/admin.js`：新增 `markTrimmedJudges(judges, resultRows)`，
  由 `buildJudgeDetail(config.dimensions, result.rounds)` 在返回前调用，
  给被去掉的 cell 打上 `trimmedAs`（`high`/`low`）与 `trimTied`
- `public/admin.js`：明细行按 `cell.trimmedAs` 加 `is-trimmed` 类，
  并在登录码右侧插入「（最高分）/（最低分）」
- `public/admin.css`：`.is-trimmed` 的横线与压暗，`.dt-trim-tag` 的标签样式

**为什么身份在路由层反查，而不是让 `scoring.js` 直接给出**：
`scoring.js` 的去分作用在**一列数值**上（`sorted.slice(1, -1)`），那一层里根本没有
「评委」这个概念 —— 它只需要去掉一个最小的和一个最大的。给它塞进身份会改变它的
输入模型，而那是整数排名确定性的核心（§6.4），不值得为显示去动它。

反查成立的前提（改动任一处都要回来核对）：两边算的都是 Σ(权重 × 分值)，
且都**只算完整票**；`judgeDetail` 的 `total` 是 `weighted / 100`，乘回 100 取整
即还原 `scoring` 里的那个整数。`already_submitted`（409）保证一场一码一票，
所以「一位评委一行」在数据上成立。

**验证**：单元测试 23/23（未动 `src/scoring.js`）。另用真实服务的接口逐项核对：
- 实测数据里只有第 2 位演讲者一场是 3 票（`trimmed: true`，`removedLow: 275`、
  `removedHigh: 425`）。接口返回该场两位评委 `2.75 → low`、`4.25 → high`，
  另两场 2 票的**一个都没标**
  （⚠️ 不写真实登录码：仓库是公开的，而登录码就是投票凭据）
- 对**文件里那段真实函数文本**做边界用例（`new Function` 抠出来跑，不是另写一份副本）：
  最低分并列 → 标一个并置 `tied`；最高分并列 → 同上；仅 2 票 → 不标；
  含残缺票（`total: null`）→ 该票不占去分名额；三票全同 → 一对一并都置 `tied`；
  同分场次连跑两次 → 标的是同一行（定序稳定）
- 浏览器实测（1280×900，亮色与暗色各一遍）：横线横跨整行含登录码格，
  分值压暗而登录码保持清晰，标签可读
