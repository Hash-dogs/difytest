# 演讲比赛「一场一投」打分系统 —— 项目方案（PLAN）

> 2026-09-18 起，本项目从「一次性打完所有人」**完全替换**为**演讲比赛现场模式**。
> 旧模式的设计（11 名参赛者 × 5 维度一次性提交、不允许弃权、内网单机部署）已全部作废。
>
> 源码里的注释会写 `PLAN §x.y` 指向本文档对应小节，改结构时请同步改引用。

---

## 1. 项目背景与目标

一位演讲者上场就是一个时段（下称**一场**）：

- 一场之内，每位评委的手机上**只显示当前这位演讲者**的打分页，5 个维度各打 1–5 分。
- 提交后评委只看到等待页，**看不到任何其他演讲者的信息**，也没有上一位/下一位导航。
- 主持人（管理员）决定何时切到下一位；**随时可切**，未提交的评委记为**弃权**。

评委用手机在**企业微信**里点链接访问，因此部署形态是公网服务器而不是内网笔记本
（内网方案此前已判定存在 WiFi AP 客户端隔离的单点阻断风险）。

---

## 2. 锁定决策表

| # | 项目 | 决定 | 影响 |
|---|---|---|---|
| 1 | 推进模型 | 管理员**随时可切**，未提交的记为弃权 | 决定状态机，见 §7.2 |
| 2 | 计分 | **自适应去分**：每格按实际票数去分，票数不足时降级 | 见 §6 |
| 3 | 匿名边界 | **强匿名**：`ballot` 只有 `ballot_id` 一列、`WITHOUT ROWID`、与短码表无任何关联键 | 见 §5.2 |
| 4 | 登录 | **统一入口 `/v` + 手输登录码**；码由后台**预先批量签发**，管理员一对一发放 | 见 §7.1 |
| 5 | 登录码备注 | **不加备注名**，管理员另存一份对应表 | 数据层少一列身份信息 |
| 6 | 评委人数 | 后台可配的**固定值**，作为「已收 X / N」的分母 | 上限 20，见 §6.3 |
| 7 | 彩排 | 提供**一键重置**（清场次与选票，保留名单/维度/登录码） | 见 §8.3 |
| 8 | 部署 | 腾讯云轻量应用服务器，公网 IP 直连，不备案 | 见 §3、§4 |
| 9 | 提交 | **每场终局**，不可修改；重开 = 为该演讲者新开一场 | 见 §5.4 |
| 10 | 平局 | 允许并列，下一名跳号（标准竞赛排名） | 见 §6.3 |
| 11 | 结果可见性 | 管理员随时可见；比赛期间靠横幅自觉不泄露 | 见 §8.4 |
| 12 | 数据导出 | 保留 Excel 导出（汇总 / 场次明细 / 计分说明三张表） | 见 §8.4 |

---

## 3. ⚠️ Day-0：先验证服务器，再写代码

**这一步不通，后面全部作废。** 顺序：

1. 注册腾讯云 → **实名认证**
2. 购买**轻量应用服务器**，内地节点，最低配
3. 防火墙/安全组放行 TCP **3000**
4. 把现有版本直接部署上去，**先不改代码**
5. 实测三件事：
   - 手机 **4G/5G** 打开 `http://<公网IP>:3000/v` —— 验证腾讯云没有拦非标准端口
   - **在企业微信里点这个链接** —— 验证企微没有弹风险提示或拦截页
   - 确认打开后落在**企微内置浏览器**还是**系统浏览器**（见 §12 R3）

**判定**：三项全过 → 按本方案走。**任一不过 → 立刻改买腾讯云香港节点**
（同样几步就能开，免备案、合法、无拦截风险；代价是跨境公网质量不保证，
但 20 人提交几百字节 JSON 对延迟不敏感，真正的风险是丢包，所以香港节点也必须当场实测）。

---

## 4. 部署方案

**选型**：腾讯云轻量应用服务器（Lighthouse），内地节点，公网 IP 直连，**不使用域名**。
不备案的前提下，域名指向内地服务器一定会被拦；直接用 `IP:端口` 不经过域名体系。
这属于灰色地带，所以 §3 的验证是前置条件。

**必须做的三处适配**：

1. **公网地址注入**。`src/net.js` 的 `entryUrls()` 在没有配置时用
   `os.networkInterfaces()` 拼地址 —— 在云服务器上那只会返回内网 IP（`10.x`），
   二维码会指向评委根本连不上的地址。所以设置环境变量 **`PFXT_PUBLIC_URL`**
   （如 `http://123.45.67.89:3000`），它优先返回；局域网地址排在后面仅供本机核对。
   启动横幅会明确打印实际生效的公网地址，便于一眼核对。
2. **常驻运行**。`pm2 start src/server.js --name pfxt` + `pm2 save` / `pm2 startup`。
   `start.sh` 保留作开发用。
3. **端口与信任代理**。`PORT=3000`、`HOST=0.0.0.0` 不变；
   `app.set('trust proxy', false)` **不要改** —— 直连场景下 `req.ip` 就是真实来源 IP，限流依赖它。

**无 HTTPS 的影响已核查，是安全的**：`public/admin.js` 已用 `window.isSecureContext`
判断过 `navigator.clipboard`，纯 HTTP 下自动走 `execCommand` 回退；
客户端代码没有使用 `crypto.randomUUID`（该 API 同样要求安全上下文）。
服务端的 `crypto.randomUUID` 在 Node 里不受影响。

---

## 5. 数据模型

### 5.1 表结构

```sql
contestant (id, seq, name, project, intro)                    -- 演讲者名单
dimension  (id, seq, name, weight, detail)                    -- 评分维度

-- 登录码表：只回答「这个码是否存在 / 是否作废」，绝不指向选票
invite (code PK, created_at, revoked)

-- 场次：一位演讲者一个时段。同一演讲者可以有多场（重开场景，见 §5.4）
round (id PK, contestant_id, seq, status, opened_at, closed_at)
    + 部分唯一索引 round_single_live ON round(status) WHERE status='live'

-- 提交记录：与 ballot 无任何关联键。这是「谁交了第几场」的唯一来源。
round_submission (round_id, code, submitted_at, PK(round_id, code)) WITHOUT ROWID

-- 选票表：⚠️ 只允许有 ballot_id 这一列
ballot (ballot_id PK) WITHOUT ROWID

-- 评分行：用 round_id 归属场次
score (ballot_id, round_id, dimension_id, value, PK(ballot_id, round_id, dimension_id)) WITHOUT ROWID

setting (key PK, value)
```

**`round_single_live` 是关键**：部分唯一索引让「同时最多只能有一个进行中的场次」
由**数据库**保证，而不是靠 `advance()` 自觉 —— 管理员连点两次、或以后加了别的入口，
都不可能造出两个 live 场次。

**相对旧库的变化**：`invite` 的 `status`/`opened_at`/`submitted_at` 三列删除；
`score.contestant_id` 改为 `round_id`（重开场景下同一演讲者有多场，用 contestant_id 分不开）；
新增 `round` 与 `round_submission`。

**老库拒绝启动**：`PRAGMA user_version` 不是 2 时直接抛错并给出处理步骤。
不做自动迁移 —— 旧的「一次性提交」选票无法映射到任何场次，
静默转换只会让人误以为数据还在。

### 5.2 ⚠️ 匿名性约束（代码评审重点）

核心：**登录码表只回答「哪个码交了第几场」，选票表只回答「有人打了这些分」，
两表之间没有任何键可 join。**

1. `ballot` 禁止出现 `code` 列，禁止任何指向 `invite` 的外键
2. `ballot` 禁止存任何时间戳
3. `ballot` / `score` / `round_submission` 必须 `WITHOUT ROWID` ——
   否则隐式 rowid 按插入顺序增长，第 3 张票就是第 3 个提交的人
4. 提交接口禁止把 code / IP / User-Agent / 请求时间写入 `ballot` 或 `score`
5. `round_submission` 的列集合**严格等于** `{round_id, code, submitted_at}`
6. `round_submission` 禁止出现 `ballot_id` 或任何指向 `ballot` 的外键
7. `score` 的列集合**严格等于** `{ballot_id, round_id, dimension_id, value}`。
   允许 `round_id` 是因为它标识的是**场次**而不是评委 —— 由
   `round_submission(round_id, code)` 与 `score(ballot_id, round_id)` 只能得到
   「这场有哪些码交了」和「这场有哪些票」，是**集合对集合，不是一一映射**
8. `round` 与 `invite` 禁止出现 `ballot_id`

`src/db.js` 的 `assertAnonymousSchema()` 在每次启动时按这 8 条逐条断言，
并把列名检查做成**集合精确相等**（不是「不多于」），另外禁止库中存在
view / trigger（视图能悄悄 join 两张表，绕过上述所有检查）。违反即拒绝启动。

### 5.3 ⚠️ 本模型固有的边界（必须如实告知，不能藏）

> **某一场若只有 1 个评委提交，那一场唯一的那张选票必然出自这位评委。**

这是「一场一投 + 主持人随时切换」模式的**固有性质**，任何实现都回避不了，
不是本项目的缺陷。处理方式是把它摆在明面上而不是藏起来：

- 结果页对票数 ≤ 2 的场次加「薄数据」角标，横幅列出具体是第几场
- `keptCount === 1` 的单元格标「仅 1 票」
- Excel 的「计分说明」页以书面形式写明这条边界

另一条既有残留：`round_submission.submitted_at` 让管理员能看到**提交顺序**。
选票本身没有任何时间戳，无法把某张票对应到某人，但在场次颗粒度上先后顺序是可观察的。
**对外说明时不能声称「连提交顺序都不知道」。**

### 5.4 重开一场的语义

- 重开**不是**把旧场次的 `status` 改回 `live` —— 那会让已经交过的评委被允许再交一张，
  票数凭空翻倍，而且新旧票在库里分不开（同一 `round_id`）。
- 重开 = **为同一位演讲者新建一场**（`seq` 更大）。计分时只取该演讲者 `seq` 最大的那一场，
  旧场进 `superseded`，在后台置灰展示、进导出备注、**不计入排名**。

---

## 6. 计分规则（核心算法）

### 6.1 旧规则为什么不成立

旧算法靠「不允许弃权」：所有评委给每一格都打了分，所以每格票数恒等于 N，
于是 `k = N - 2` 对**所有单元格是同一个常数**，`U_c = Σ(w_d × sum)` 是整数，
旧版才敢规定「排名只能比较整数，禁止比较浮点数」。

管理员可随时切之后弃权成了常态，**每一场的票数都可能不同**，`k` 不再是常数。

### 6.2 新规则：自适应去分

对每个 `(场次, 维度)` 单元格，设实际收到的分数为 `raw`：

| `raw.length` | 处理 | 标记 |
|---|---|---|
| ≥ 3 | 升序排序，去掉首尾各一个，其余取平均 | 正常 |
| 2 | 两个分数直接取平均（去分会把分数去光） | — |
| 1 | 直接用这个分数 | `single: true`，页面标「仅 1 票」 |
| 0 | 该场不计分 | 整行 `blank`，`total = null`，排名 `null` |

**每一场都必须产出结果**，票数再少也不抛错、不阻塞导出。

### 6.3 ⚠️ 排名仍必须用整数：LCM 缩放

分母不再相同之后 `U` 变成有理数。为了不退回浮点比较，把分母统一到
`L = LCM(所有出现过的 keptCount)`：

```
uScaled = Σ_d  w_d × sum_d × (L / keptCount_d)      ← 整数，排名只比较它
total   = uScaled / (WEIGHT_TOTAL × L)              ← 仅用于展示
```

**这个式子在「所有场次票数相同」时精确退化成旧公式**（此时 `L = keptCount`，每项乘 1），
所以新算法是旧算法的严格推广，不是另起一套。`test/scoring.test.js` 里有一条专门的
「退化等价」用例钉住这个性质。

**量级证明**：评委人数上限 `MAX_JUDGES = 20`，`keptCount ∈ [1,20]`，
`LCM(1..20) = 232,792,560`。单项上界 `w_d(≤100) × sum_d(≤5×20=100) × L/1` ≈ `2.33e12`，
5 个维度求和 ≤ `1.2e13`，远小于 `Number.MAX_SAFE_INTEGER`（`9.007e15`）。
实现里对 `L` 设了 `LCM_LIMIT` 硬闸，并对每个 `u` 断言 `Number.isSafeInteger` ——
超出设计前提时**宁可拒绝出结果，也不要算出悄悄丢精度的排名**。

**为什么不能只比 `toFixed(2)`**：测试里有一条构造用例——
两场总分都是 `"1.05"`，但整数 `u` 分别是 40000 和 39900，名次必须分开。

### 6.4 函数签名

```js
computeResults({ rounds, dimensions, ballots })
// → { rounds: [{ roundId, contestantId, seq, name, project,
//                n, thin, blank, margins, details, u, total, rank }],
//     lcm, orphans, insufficient }

pickEffectiveRounds(rounds)  // 同一 contestantId 只保留 seq 最大的一场
lcm(values)
```

---

## 7. 评委端

### 7.1 登录：统一入口 + 手输短码

- 登录码由管理员在后台**批量预先生成**（`POST /api/admin/invites`），一对一发给评委
- 统一入口 `/v` 是**登录码输入页**，不自动发码（旧版的 `/api/v/claim` 已删除）
- 输入规范化：去空白与连字符、统一大写。字符集不含 `I/O/0/1`，对手输友好
- 登录成功后码存 `localStorage` 并带进 URL（`/v/<code>`）

**⚠️ 短码因此从「随机标识」变成了「持有即可用的凭据」**：旧流程里猜不中可以直接领一个新的，
现在猜中就能冒名投票。所以 `/api/v/login` 单独限流。

**限流额度刻意不是 10 次**：现场 20 个评委多半连同一个公司 WiFi，出口 NAT 后是
**同一个公网 IP**。按 10 次/10 分钟算，几个人手滑打错就能把全场锁在门外。
真正的防线是短码空间 `32^8 ≈ 1.1e12`（暴力枚举不可行），
限流只是挡住脚本扫描的减速带，所以取 **60 次 / 10 分钟 / IP** 并记录失败日志。

### 7.2 状态机

| 状态 | 触发条件 | 界面 |
|---|---|---|
| `loading` | 刚打开 | 转圈 |
| `blocked` | 码无效 / 已作废 / 投票已结束 / 连不上 | 阻断页（无效时可点「重新输入登录码」） |
| `waiting` | 已登录，当前没有 live 场次 | 「等待主持人开始下一位」+ 轮询 |
| `score` | 有 live 场次，本场未提交 | ★ 唯一的打分屏，**只显示当前这一位** |
| `submitted` | 有 live 场次，本场已提交 | 「本场已提交，等待下一位」 |

**「看不到其他演讲者」由三层保证**：

1. **服务端**：`/api/v/:code/state` 的响应体里**只有当前这一位**，
   绝不返回 `contestants` 数组。验收用 `JSON.stringify` 断言响应体不含其他演讲者姓名。
   维度也不下发 `weight` —— 评委不需要看到权重，那只会让打分时先在心里加权一遍。
2. **前端**：没有上一位/下一位/跳转控件，也没有位置记忆。
3. **数据**：任何人的分数本来就不下发到评委端。

**提交后不回放自己的分数**：`submitted` 屏只说「已提交」，不展示打过的分。
这是对「提交后也无法查看上一个演讲者的得分信息」最保守的解读。

**换场时若上一场有没提交的草稿**，新场次顶部会出现一条提示
「上一场已经结束了，你没来得及提交，那一场记为弃权」——不能悄悄吞掉。

### 7.3 轮询，不用 WebSocket / SSE

打分页与等待页每 **3 秒** 轮询一次 `/api/v/:code/state`。

理由：企业微信内置浏览器对 `ws://` 支持不佳（官方社区明确建议用 `wss://`），
而当前部署是纯 HTTP，`wss` 无从谈起；SSE 在该环境下的支持情况查不到任何官方说法。
轮询还能天然熬过「WebView 切后台」「链接改在系统浏览器打开」这类上下文切换。
负载上，20 人 × 20 场 × 每 3 秒一次对一台 2 核机器完全不构成压力。

**上下文切换的应对**：同一个链接在企微内置浏览器和系统浏览器里是两个独立的存储域，
localStorage 不互通。所以短码**同时**存在于 URL 路径与 localStorage 中，
读取顺序是 **URL 优先 → localStorage 兜底 → 都没有才回输入页**。

**网络失败时不替换正在打分的界面**：草稿存在本机，只在提示行说明「网络不稳，正在重试」。
贸然切屏会把评委正在打的分从眼前拿走。

### 7.4 草稿

只存浏览器本地，服务端不存半成品。键名按场次隔离：

- `pfxt:code` —— 登录短码
- `pfxt:draft:<CODE>:<ROUND_ID>` —— 本场未提交的打分

换一位演讲者就是一份全新的草稿，上一场没交的草稿不会被下一场复用（那会张冠李戴）。

---

## 8. 管理端

### 8.1 场次控制（默认页）

顶部控制条：当前场次 + 「已收 X / N」+ **开始下一位** 按钮。
下方是场次记录列表（倒序）、跳到指定演讲者、登录码概况、危险操作区。

- **已收 X / N**：`N` 取自「评委人数」配置项
- **超员告警**：某场提交数 **大于** 评委人数时高亮 —— 通常意味着有人清掉浏览器缓存后
  又拿到了新的登录码，必须当场核对。这是自服务登录模型下唯一的异常信号。
- **薄数据标注**：票数 ≤ 2 的场次标出来
- **重开**：见 §5.4
- 名单跑完后「开始下一位」自动禁用

### 8.2 配置锁定

**判据是「有没有开过场」**，不是「有没有选票」—— 开了场就意味着已经有评委会看到打分页。

- 开赛后：**维度集合与权重冻结**（否则可以在场次之间偷改权重，
  追溯性改变已完成场次的分值）；前端把对应输入框禁用，别让人白填一遍再被拒
- 允许：改活动名称、姓名、项目名、简介；**允许中途加人**（临时补位是真实需求）
- 禁止：删除**已经上过场**的演讲者（那会让 `round` 关联不到人、分数变成孤儿）。
  要退出就别再给他开场次。

### 8.3 一键重置（彩排用）

`POST /api/admin/reset`，要求请求体带字面量 `{ "confirm": "RESET" }`。

清空 `round` / `round_submission` / `ballot` / `score`；
**保留** `contestant` / `dimension` / `invite` / `setting`。
登录码不会失效 —— 否则彩排完还得把所有码重发一遍。

### 8.4 结果页

- 按 `uScaled` 整数比较排名，并列名次相同、下一名跳号
- **薄数据横幅**列出票数 ≤ 2 的具体场次，并说明「票数越少，去一高一低的保护越弱；
  某场只有 1 票时，那一场等于由一位评委决定」
- 单元格级「仅 1 票」角标
- 0 票的场次排末尾、总分显示「—」、**不阻塞导出**
- 比赛进行中沿用 §8.4 的横幅提醒（「请勿对外透露任何分数」）
- Excel 三张表：汇总 / 场次明细 / 计分说明。**计分说明页用书面形式写明 §5.3 的两条边界**

---

## 9. 后端 API

### 评委端（`src/routes/vote.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/v` | 登录码输入页 |
| GET | `/v/:code` | 投票页（码无效也返回页面，由前端判定，好显示中文提示） |
| POST | `/api/v/login` | 校验登录码 → `{ ok, code }`。严格限流（§7.1） |
| GET | `/api/v/:code/state` | 评委端唯一数据来源。**只含当前演讲者，不含 contestants 数组** |
| POST | `/api/v/:code/submit` | `{ roundId, scores: [{dimensionId, value} × |D|] }` |

提交事务在事务内重查「这一场还是不是当前 live 场」与 phase，
既挡住并发双击，也挡住「管理员恰好在你提交时切了场」；
`round_submission` 的主键 `(round_id, code)` 是最后一道防线。

### 管理端（`src/routes/admin.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/rounds` | 场次总览 + 登录码概况 + 异常告警（取代旧的 `/progress`） |
| POST | `/api/admin/rounds/advance` | `{ contestantId? }`，不传则按 seq 取下一个没上过场的 |
| POST | `/api/admin/rounds/:id/reopen` | 为同一演讲者新建一场（§5.4） |
| POST | `/api/admin/reset` | 一键重置（§8.3） |
| GET | `/api/admin/results` `/.xlsx` | 按 §8.4 |
| GET | `/api/admin/invites` | 含 `rounds_submitted` / `last_submitted_at` |
| POST | `/api/admin/invites/:code/revoke` | 交过任一场则拒绝作废 |
| PUT | `/api/admin/config` | 含 `judgeCount`，开赛后锁见 §8.2 |
| — | `/login` `/logout` `/session` `/entry` `/qrcode.svg` `/backup` `/close` `/reopen` | 不变 |

---

## 10. 文件与实施顺序

| 文件 | 职责 |
|---|---|
| `src/db.js` | 建表、老库拒绝、**启动期匿名性断言**（§5.2 的 8 条） |
| `src/scoring.js` | 纯函数计分：自适应去分 + LCM 缩放（§6） |
| `src/rounds.js` | 场次读写：`getLiveRound` / `advance` / `reopenRound` / `resetAll` |
| `src/routes/vote.js` | 评委端接口 |
| `src/routes/admin.js` | 管理端接口 |
| `src/net.js` | `PFXT_PUBLIC_URL` 支持 |
| `src/seed.js` | 默认名单 / 维度 / `judge_count` / 管理员口令 |
| `public/entry.html` | 登录码输入页 |
| `public/vote.{html,js}` | 评委端状态机（§7.2） |
| `public/admin.{html,js}` | 管理后台，默认落在「场次」页 |
| `test/scoring.test.js` | 计分单元测试 |
| `test/acceptance.js` | 端到端验收（§11） |

---

## 11. 验收清单

### 单元（`npm test`）

- 自适应去分 `n=1/2/3/5/6` 的 `keptCount` 与 `margin` 字面量
- **跨场次混合**：11 票（keptCount 9）与 2 票（keptCount 2）同场，`L = LCM(9,2) = 18`
- **浮点陷阱**：两场总分都是 `"1.05"` 但整数 `u` 不同 → 名次必须分开
- **退化等价**：所有场次票数相同时，新公式精确等于旧公式 `Σ(w×sum)`
- **溢出上界**：keptCount 覆盖 1..20 时 `L = 232,792,560`，`u` 仍是安全整数
- 越界拒绝、弃权不抛错、空白场次、孤儿行、重开取最新场

### 端到端（`PFXT_PASSWORD=<口令> npm run acceptance`）

> 会写库并**清空演练数据**，不要在正式数据上跑。

- **匿名性结构检查**（直读 db 文件）：三张表的列集合精确相等、`WITHOUT ROWID`、
  `ballot_id` 不出现在别处、无指向 `ballot` 的外键、无 view/trigger、
  `ballot` 行数 == `round_submission` 行数
- **★ `/state` 响应体不含其他演讲者姓名**、不返回 `contestants`、不下发权重
- 登录规范化（小写/空格/连字符）、错误码 404、限流最终触发
- 场次主流程：开场 → 提交 → 重复提交 409 → 推进 → 旧 `roundId` 409 `round_closed`
- 弃权：3 个码只交 2 个就推进 → 该场 `n=2`、`thin`、计分不抛错
- 同码交两场 → `COUNT(DISTINCT ballot_id) = 2`
- 封盘 / 重新开放；重开一场；一键重置后**登录码仍然有效**
- 连点 advance 不产生两个 live 场（数据库层已由唯一索引保证）

### 真机彩排（**不可省**）

1. 两台手机（iOS + Android）**在企业微信里**点链接，确认打开的是哪个浏览器
2. 走完「输码 → 打分 → 提交 → 等待 → 主持人切换 → 新场自动出现」
3. 手机息屏 5 分钟再唤醒，确认轮询恢复、登录态还在
4. 确认企微与系统浏览器两个上下文里短码能否互认（预期：不能，走 URL 兜底）
5. 跑一次一键重置

---

## 12. 风险与开放项

| # | 风险 | 处理 |
|---|---|---|
| R1 | **腾讯云拦截内地 IP:端口** | §3 的 Day-0 验证；不通立即换香港节点。**最高优先级** |
| R2 | **企微对未备案链接弹风险提示** | Day-0 就要在企微里实测点击 |
| R3 | **企微内置浏览器 vs 系统浏览器存储域不互通** | 短码 URL 优先 + localStorage 兜底（§7.3） |
| R4 | **企微有概率清掉 localStorage** | 同上，URL 兜底让这件事变成「重输一次短码」而不是「登录丢失」 |
| R5 | **某场只有 1 票 → 该票主人可被推出** | 薄数据标注（§5.3）；写进给评委的说明，不宣称「连顺序都不知道」 |
| R6 | **短码成为凭据后可被猜** | 限流 + 失败日志（§7.1）；主防线是 32^8 的密钥空间 |
| R7 | **清缓存换码重复投票** | 结构上无法识别（没有身份）。后台「超员」告警把异常摆到眼前 |
| R8 | **管理员_session 是进程内 Map** | 单机 + pm2 单实例下没问题。**若以后迁 Serverless 会随机掉线** |
| R9 | **`PFXT_PUBLIC_URL` 配错 → 二维码指向内网 IP** | 启动横幅打印实际生效的公网地址 |
| R10 | **管理员误点「开始下一位」** | 用「重开这一场」补救（§5.4）；彩排时验证这条路径 |

---

## 13. 沿用不变的部分

- **管理员口令**：哈希 + 明文双存、每次启动打印、`npm run reset-password` 命令。
  哈希不可逆，没有明文就没法「启动即见口令」，这是刻意的取舍。
  老库（只有哈希、没有明文）启动时自动重新生成一个并**显式告警「旧口令从此刻起失效」**。
- **匿名性**：`ballot` 单列 + `WITHOUT ROWID` + 启动期结构断言 —— 本次是**加强**，不放宽
- 草稿只存浏览器本地，服务端不存半成品
- 提交即终局（现在是「每场终局」）
- 平局并列、下一名跳号
- 管理员随时可见分数（靠自觉不泄露）
- 零构建步骤、原生 HTML/CSS/JS；依赖仍只有三个：express、better-sqlite3、qrcode-generator

---

## 14. 2026-09-18 实施记录（与初版方案的偏差）

| 项 | 方案 | 实施 | 原因 |
|---|---|---|---|
| 登录限流 | 10 次 / 10 分钟 / IP | **60 次 / 10 分钟 / IP** | 现场评委共用公司 WiFi 出口 NAT，同一个公网 IP。10 次会被几个手误打光，把全场锁在门外 |
| 单 live 场次 | 靠 `advance()` 判断 | **数据库部分唯一索引** `round_single_live` | 把不变量交给数据库，管理员连点或将来加新入口都造不出两个 live 场 |
| 计分说明页 | 只写匿名说明 | 增加**「必须如实告知的边界」**两条 | §5.3 的固有性质必须书面留痕，否则导出文件会声称一个系统给不出的承诺 |
| 老库处理 | 「拒绝启动」 | 明确到 `PRAGMA user_version`，并给出可执行的恢复步骤 | 光说「拒绝」没用，得告诉人怎么办 |

**验证情况**：单元测试 14/14 通过；端到端验收 91/91 通过；
管理端与评委端已在真实浏览器（Chromium，手机宽度 420px）跑通完整流程，
包括「管理员在评委打分途中切场 → 评委页 3 秒内自动切换并显示弃权提示」。

**尚未完成**：§3 的 Day-0 服务器实测（需要先购买腾讯云实例）。
这是唯一的阻断项，**必须在写任何部署配置之前完成**。
