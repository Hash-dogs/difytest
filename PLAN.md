# 评委匿名打分系统 —— 项目方案（PLAN）

> 本文档由一轮需求追问收敛而成，用于驱动后续项目生成。
> 文档中的每条决定都经过确认；带 ⚠️ 的条目是**强约束**，改动前必须重新评估连带影响。

---

## 1. 项目背景与目标

内网临时使用的**匿名评委打分系统**，用于内部项目/系统评比。

- **参赛者**：3 名（可配置，首次运行按《Dify 工作流参赛名单》写入）
- **评委**：11–20 人（可配置），与参赛者**完全分离**，不存在自评
- **打分**：5 个维度，每项 1–5 分整数
- **评委端**：手机浏览器为主，匿名打分
- **管理端**：生成投票 URL、监控进度、按规则计分并展示看板
- **生命周期**：一场活动，用完可复用（配置可改，但不做多场并存）

---

## 2. ⚠️ Day-0 阻断验证（写代码之前必须完成）

整个方案依赖**手机能访问跑在内网笔记本上的服务**。企业 WiFi 普遍开启客户端隔离（AP isolation）、或把无线放在独立 VLAN；Windows 防火墙默认拦截 3000 端口入站；组策略可能禁止放行。**任何一条成立，项目直接归零。**

验证步骤（约 10 分钟）：

```powershell
# 1) 笔记本上起一个临时 HTTP 服务
node -e "require('http').createServer((q,s)=>s.end('OK')).listen(3000,'0.0.0.0',()=>console.log('listening on 3000'))"
```

```powershell
# 2) 查本机局域网 IP（替换下面的地址）
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -notlike '127.*' }
```

3) **手机连同一个 WiFi**，浏览器打开 `http://<笔记本IP>:3000`

- 看到 `OK` → 方案成立，继续开发
- 打不开 → 先解决网络（有线同网段 / 找 IT 放行 / 换公网主机），**不要继续开发**

> 2026-09-16 记录：验证机在 `WLAN 10.1.21.33`（DHCP）。

---

## 3. 锁定决策表

| # | 项目 | 决定 | 影响 |
|---|---|---|---|
| 1 | 匿名边界 | 邀请码表只记「是否已提交」，选票独立存储、**无任何关联键** | 决定数据模型 |
| 2 | 草稿保存 | 只存评委浏览器 `localStorage`，服务端不存半成品 | 简单、不泄露 |
| 3 | 部署 | 内网单机，评委 11–20 人，手机为主 | 无需 HTTPS |
| 4 | URL 分发 | **一个固定二维码入口 `/v`**，评委扫码后由服务器当场签发专属 token（存该评委浏览器）；后台仍保留手工批量生成链接的能力 | 见 §6.3 |
| 5 | 计分 | 5 维度权重 30/25/20/15/10；**按维度**各去一高一低取平均，再加权 | 见 §4 |
| 6 | 弃权 | **不允许**，55 个评分点必须全部打分 | 见 §4.3 |
| 7 | 提交后 | 终局，不可修改 | 见 §8.3 |
| 8 | 参赛者信息 | 姓名 + 项目名 + 简介 | 保证数据质量 |
| 9 | 平局 | 允许并列，下一名跳号（标准竞赛排名） | 见 §4.5 |
| 10 | 结果可见性 | 管理员**随时**可见分数（自觉不泄露） | 见 §7.4 |
| 11 | 数据导出 | **不做**报表导出，看板够用 | 保留数据库备份 |
| 12 | 复用 | 后台可配置，只支持一场活动 | — |
| 13 | URL 管理 | 填个数批量生成，可作废**单个未提交**的链接 | 见 §6.2 |

---

## 4. 计分规则（核心算法）

### 4.1 参数

- 参赛者 `c ∈ C`，评委提交的有效选票数 `N`
- 维度 `d ∈ D`，权重 `w_d`（**整数**，默认 30/25/20/15/10，约束 `Σ w_d = 100`）
- 每个单元格 `(c, d)` 的原始分集合 `S_cd`，每个元素 ∈ {1,2,3,4,5}

### 4.2 计算步骤

```
N = count(invite WHERE status = 'submitted')        # 有效票数 = 实际提交数

FOREACH (c, d):
    S = { score.value WHERE contestant=c AND dimension=d }
    assert |S| == N                                  # §4.3 保证，无需处理缺失

k  = N - 2                                           # 一律去分，不再按 N 分档（见 §4.4）
S' = S 去掉一个最小值和一个最大值                     # 各去一个，不是去所有极值

    m_cd = sum(S') / k                               # 该维度去分后均分 ∈ [1, 5]

Total_c = Σ_d (w_d × m_cd)                           # 加权总分 ∈ [1, 5]
```

### 4.3 ⚠️ 「不允许弃权」是这套算法的前提

因为不允许弃权，**每个单元格的有效票数恒等于 N**，不存在「某个维度只有 3 张票」的情况，去分规则没有边界情况。

> 如果将来放开弃权，`|S_cd|` 会逐单元格浮动，`k` 不再统一，§4.5 的整数排名技巧失效，边界情况会大量涌现。**改这一条等于重做算法。**

### 4.4 一律去分（原保底规则已于 2026-09-16 删除）

> **变更记录**：本节原为「`N < 6` 时不去分」的保底规则，2026-09-16 按需求删除。
> 现在**无论多少票都去掉一个最高分和一个最低分**。下面是删除后确认接受的边界后果。

`N` 由**实际提交数**决定，不是「计划评委数」。

| N | k = N−2 | 后果 |
|---|---|---|
| 0 | — | 看板显示「数据不足」，不排名（没有数据，与去分规则无关） |
| 1 | −1 | 去分后无剩余分数 → 全员 0.00，并列第 1 |
| 2 | 0 | 去分后无剩余分数 → `0/0 = NaN`，序列化为 `null`，页面显示「—」，全员并列第 1 |
| 3 | 1 | 只剩 1 个分数 → **等于由单个评委决定名次** |
| 4+ | N−2 | 正常 |

⚠️ N ≤ 2 不产出有意义的结果，N = 3 统计意义很弱。这是「一律去分」的直接代价，**已确认接受，不做特判**。

### 4.5 ⚠️ 排名必须用整数，禁止用浮点数比较

因为所有单元格的 `k` 相同（见 §4.3），可以把它提出来：

```
U_c = Σ_d (w_d × sum(S'_cd))        # 整数！
Total_c = U_c / (100 × k)           # 仅用于展示，保留 2 位小数
```

**排名按整数 `U_c` 降序比较**。好处：

- 并列判定是**精确的整数相等**，没有浮点误差导致的「显示同分但名次不同」
- 展示保留 2 位小数、排名用全精度，两者天然一致

名次编号（标准竞赛排名，并列跳号）：

```
sort by U_c DESC
rank[0] = 1
rank[i] = (U_c[i] == U_c[i-1]) ? rank[i-1] : i + 1

例：U = [4590, 4310, 4310, 4100]  →  名次 = [1, 2, 2, 4]
```

### 4.6 已知的口径副作用（记录备查，非缺陷）

- **权重放大不对称**：`w=30` 的维度被剔一次极值，对总分的影响是 `w=10` 维度的 3 倍。
- **去分治不了「整体手松/手紧」**：一个给所有人在所有维度都打 5 分的评委，只有在**唯一最高**时才会在每个维度被各剔一次；一旦与他人并列最高就剔不掉。去分只削尖峰，不平整体尺度。
- **强制全评会产生编造分**：没看全 11 个项目的人被迫打分。去分能削掉一部分极端编造值，但削不干净。**技术手段之外，应在评委须知里明确要求「基于实际了解打分」。**

---

## 5. 数据模型

SQLite 单文件。**§5.2 的匿名约束是整套匿名性的全部依托，任何新增字段前必须重读。**

### 5.1 表结构

```sql
-- 参赛者（后台可配）
CREATE TABLE contestant (
  id       INTEGER PRIMARY KEY,
  seq      INTEGER NOT NULL,          -- 展示顺序
  name     TEXT    NOT NULL,
  project  TEXT    NOT NULL,          -- 项目/系统名
  intro    TEXT    NOT NULL DEFAULT ''
);

-- 评分维度（后台可配）
CREATE TABLE dimension (
  id       INTEGER PRIMARY KEY,
  seq      INTEGER NOT NULL,          -- 默认 1..5
  name     TEXT    NOT NULL,
  weight   INTEGER NOT NULL,          -- 整数，合计必须 = 100
  detail   TEXT    NOT NULL DEFAULT '' -- 核心评价内容（前端要展示给评委）
);

-- 邀请码表：只回答「谁投了」，绝不回答「投了什么」
CREATE TABLE invite (
  code         TEXT PRIMARY KEY,      -- 随机短码，即 URL 里的那串
  status       TEXT NOT NULL,         -- issued | opened | submitted | revoked
  created_at   INTEGER NOT NULL,
  opened_at    INTEGER,               -- 只记「打开过」，不记进度、不记分数
  submitted_at INTEGER
);

-- 选票表：与 invite 无任何关联
CREATE TABLE ballot (
  ballot_id TEXT PRIMARY KEY          -- 独立随机 UUID
) WITHOUT ROWID;                      -- ⚠️ 见 §5.2

CREATE TABLE score (
  ballot_id     TEXT    NOT NULL,
  contestant_id INTEGER NOT NULL,
  dimension_id  INTEGER NOT NULL,
  value         INTEGER NOT NULL CHECK (value BETWEEN 1 AND 5),
  PRIMARY KEY (ballot_id, contestant_id, dimension_id)
) WITHOUT ROWID;                      -- ⚠️ 见 §5.2

CREATE TABLE setting (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
-- admin_password_hash / phase(open|closed) / activity_name / deadline
```

### 5.2 ⚠️ 匿名性约束（代码评审重点）

1. **`ballot` 表禁止出现 `code` 列**，也禁止任何指向 `invite` 的外键。这是「只知谁投了、不知投了什么」的唯一保障。
2. **`ballot` 表禁止存任何时间戳**。管理员手上就是那个 `.db` 文件，时间戳就是关联键。
3. **`ballot` 与 `score` 必须 `WITHOUT ROWID`。** 否则 SQLite 的隐式 `rowid` 会按插入顺序增长 —— 第 3 张插入的票就是第 3 个提交的人，配合 `invite.submitted_at` 即可把票对应到人。`WITHOUT ROWID` 让行按随机主键排序存储，插入顺序不可从存储推断。
4. **提交接口禁止把 `code` 或任何请求元数据（IP、User-Agent、请求时间）写入 `ballot` / `score`。**
5. `submitted_at` 只能写在 `invite` 上，且不得与 `ballot` 的创建建立任何可对齐的序号。

> 若将来要做「导出原始票明细」，务必**不带时间戳、不带序号**，否则与 `invite` 交叉即可反推。

---

## 6. 后端 API

### 6.1 评委端（无需登录）

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/v` | **固定入口引导页**（二维码指向这里）。页面读本机 `localStorage` 里的 token，有就复用、没有就领一个新的，然后跳到 `/v/<token>` |
| `POST` | `/api/v/claim` | 领取 / 找回 token，见 §6.3 |
| `GET` | `/v/:code` | 返回投票页。若 `status = issued`，置为 `opened` 并记 `opened_at`（幂等，不覆盖已有时间）。`revoked` 返回失效页 |
| `POST` | `/api/v/:code/submit` | 提交选票，见下 |

`POST /api/v/:code/submit` 请求体：

```json
{ "scores": [ { "contestantId": 1, "dimensionId": 1, "value": 4 }, ... ] }
```

校验顺序：

1. `code` 存在，且 `status ∉ {revoked, submitted}`
2. `setting.phase == 'open'`（已封盘则拒绝）
3. 载荷恰好包含 `|C| × |D|` 条，无重复、无遗漏，每个 `value ∈ [1,5]` 整数

事务内执行：

```
ballot_id = randomUUID()
INSERT INTO ballot(ballot_id) VALUES (?)          -- 只有这一列
INSERT INTO score ... × 55
UPDATE invite SET status='submitted', submitted_at=? WHERE code=? AND status IN ('issued','opened')
```

**幂等**：依靠最后一条 `UPDATE` 的 `WHERE status IN (...)` 条件 —— 影响行数为 0 即表示已被提交过，回滚整个事务并返回 `409`。并发双击提交安全。

成功返回 `{ ok: true }`；评委端**不接收任何已有数据**（草稿在本地）。

### 6.2 管理端（口令鉴权）

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/admin/login` | 口令登录，签发会话 Cookie |
| `GET` | `/api/admin/config` | 读参赛者 / 维度 / 权重 / 活动名 |
| `PUT` | `/api/admin/config` | 改配置。**权重必须为整数且合计 = 100**；**已有提交后禁止改维度数与参赛者数** |
| `POST` | `/api/admin/invites` | `{ count }` → 批量生成随机短码，返回列表 |
| `POST` | `/api/admin/invites/:code/revoke` | 作废。**仅 `issued` / `opened` 可作废**；`submitted` 返回 `409` 并说明原因 |
| `GET` | `/api/admin/progress` | `{ issued, opened, submitted, revoked }` 计数 |
| `GET` | `/api/admin/results` | 计分结果（§4），含每维度均分、加权总分、名次 |
| `POST` | `/api/admin/close` | 封盘，`phase = 'closed'` |
| `POST` | `/api/admin/reopen` | 重新开放（需二次确认） |
| `GET` | `/api/admin/backup` | 下载 `data/pfxt.db` 文件 |
| `GET` | `/api/admin/entry` | 固定入口地址列表（局域网 IPv4 + 端口） |
| `GET` | `/api/admin/qrcode.svg` | 入口二维码 SVG。只接受本机探测到的 IP，`?ip=` 切换 |

> **`submitted` 的链接不可作废**：选票已与身份脱钩，无法定位该删哪张票。若强行作废，只会造成「标记作废但票还在算」的矛盾。UI 上必须禁用该按钮。

### 6.3 ⚠️ 固定入口与自助领取（2026-09-16 新增）

**二维码只承担「入口」，不承担「身份」。** 身份在扫码之后由服务器当场签发。

```
评委扫码  →  GET /v（引导页）
          →  POST /api/v/claim  { token?: 本机已有的 }
          →  服务器返回一个 invite.code，存进该浏览器 localStorage
          →  location.replace('/v/<code>')  之后流程与原来完全一致
```

`POST /api/v/claim` 的分支：

| 传入 token | 处理 |
|---|---|
| 不存在于库中 / 没传 | 签发新码（受 IP 限流约束） |
| 存在（`issued` / `opened`） | **原样奉还**，不签发新的 |
| 存在但 `submitted` | **原样奉还** —— 否则重扫二维码会拿到新 token，等于再投一票 |
| 存在但 `revoked` | **原样奉还** —— 让他看到「已作废」，而不是拿到一个能投票的新码 |

**⚠️ 已知取舍（已确认接受）：**

1. **防重复只能防到「同一个浏览器」。** token 只存在 localStorage，清缓存 / 换浏览器 / 换设备都会领到新码，也就等于可以再投一次。**防不住故意刷票。**
2. **无法催票。** 因为不记录「这个码是谁领的」，后台只能看到「签发 N 条、提交 M 条」，**不知道是谁没交**。
   若必须知道谁没交，须改用「投票链接」页手工为每人指定一条链接（原方案仍完整保留）。

**匿名性不受影响**：`invite` 表依旧只回答「谁投了」，`ballot` / `score` 依旧无任何关联键（§5.2 全部约束不变）。

---

## 7. 前端

### 7.1 技术形态

**原生 HTML / CSS / JS，零构建步骤。** 手机端页面不复杂，引入打包工具纯属负担。页面由 Express 静态托管。

### 7.2 评委端 `/v/:code`

**流程**：说明页 → 卡片流打分 → 二次确认 → 只读完成页

**说明页（首屏）**

- 活动名
- 5 个维度的名称、权重、核心评价内容（§5.1 `dimension.detail`）
- ⚠️ 醒目警告：**「提交后无法修改，请确认后再提交」**

**打分页**

- **一个参赛者一张卡片**，卡片内含：姓名 / 项目名 / 简介（默认展开，过长可折叠）+ 5 行维度
- 每行：维度名 + 说明文字 + **一排 1–5 按钮**
- **⚠️ 未评分必须是独立状态，绝不允许预填默认值。** 若控件预填 3 分，「全部评完才能提交」的校验就是假的（一个都不点也能过），且会把数据系统性拉向中位数。
- 顶部 sticky 进度条：`已评 3 / 11`
- 底部：`上一位` / `下一位`（最后一位变 `提交`）
- **每次点击立即写入 `localStorage`**（防抖 ≤ 300ms）

**localStorage 键**

| 键 | 内容 |
|---|---|
| `pfxt:draft:<code>` | 当前草稿 `{ [contestantId]: { [dimensionId]: value } }` |
| `pfxt:submitted:<code>` | 提交成功标记 |

**重新打开时**：有 `submitted` 标记 → 只读完成页；有草稿 → 恢复并定位到最后编辑位置；都没有 → 空白表单。

**提交校验**

1. 前端先查缺失项：有缺失则弹窗**列出缺哪几项并支持点击跳转**，不提交
2. 齐全 → 二次确认弹窗（再次强调不可修改）
3. 成功后写 `pfxt:submitted:<code>`，清草稿，切只读态

**边界：本地已提交但 localStorage 被清空**
用户清了缓存后重新打开会看到空白表单。提交时服务端返回 `409` → 前端显示「本链接已提交过，无需重复提交」。必须处理，否则用户会以为系统坏了。

### 7.3 管理端 `/admin`

- **登录**：口令
- **配置区**：参赛者列表（增删改、批量导入）、维度列表（名称 / 权重 / 说明文字）；实时显示权重合计是否为 100
- **URL 生成区**：填数量 → 生成 → 列表（短码 / 状态 / 一键复制 / 作废按钮）
- **进度区**：`已提交 14 / 20`，并按状态分列 `未打开 / 已打开未提交 / 已提交 / 已作废`
  - 「已打开未提交」是催票的关键信号：能区分「没开始」和「打到一半放弃了」
- **结果区**：排名表（每维度均分 + 加权总分 + 名次）、维度雷达图
- **封盘按钮**、**下载数据库备份**

### 7.4 ⚠️ 结果可见性

管理员**投票期间即可见分数**（已确认的选择）。风险自担：**评审群里一句「目前第一是…」就会污染剩余选票，排名会向后投者收敛（羊群效应）。**

缓解措施（必须实现）：

- 结果区顶部常驻横幅：**「投票进行中（已提交 14 / 20）— 请勿对外透露任何分数」**
- 横幅在封盘后自动变为「投票已结束」

---

## 8. 技术栈与项目结构

### 8.1 依赖

- **Node.js + Express** — 入口与路由
- **better-sqlite3** — 单文件数据库，同步 API，零运维
- 前端零依赖（不引入框架、不引入图表库；雷达图用内联 SVG 手绘）

- **qrcode-generator** — 生成入口二维码 SVG，零传递依赖（2026-09-16 新增）

**生产依赖共三个**（原为两个，二维码需求新增了第三个）。

### 8.2 目录

```
program-pfxt/
├── PLAN.md                    # 本文档
├── package.json
├── start.bat                  # 双击启动，打印局域网访问地址
├── src/
│   ├── server.js              # Express 入口 + 静态托管
│   ├── db.js                  # 建库、schema、迁移
│   ├── scoring.js             # §4 计分算法（纯函数，便于单测）
│   ├── codes.js               # 邀请码生成（评委端/管理端共用）
│   ├── net.js                 # 局域网地址探测（启动横幅 / 二维码共用）
│   ├── routes/
│   │   ├── vote.js
│   │   └── admin.js
│   └── seed.js                # 首次运行写入默认参赛者名单 / 5 维度
├── public/
│   ├── entry.html             # §6.3 固定入口引导页（二维码指向这里）
│   ├── vote.html  vote.js  vote.css
│   ├── admin.html admin.js admin.css
│   └── common.css
└── data/
    └── pfxt.db                # SQLite（首次启动自动创建）
```

### 8.3 启动

**Windows**：双击 `start.bat`
**Linux / macOS**：`./start.sh`（首次需 `chmod +x start.sh`）

两者逻辑一致：

1. 检查 Node ≥ 20
2. `npm install`（若 `node_modules` 不存在）
3. 启动服务，打印本机与**局域网**访问地址（含网卡名）
4. 首次运行自动执行 `seed.js` 并生成管理员口令，打印到控制台

### 8.4 ⚠️ Linux 部署（2026-09-16 适配）

代码本身已跨平台（全部用 `path.join`，无平台分支）。移植时真正要注意的是这三点：

**1. `better-sqlite3` 是原生模块，装它需要「网络」或「编译工具」二选一**

`npm install` 时它先尝试下载预编译二进制（需访问 GitHub），失败则回退到源码编译（需 `g++` / `make` / `python3`）。**两者都没有就会装不上，服务起不来。**

- 有外网：直接 `npm install` 即可
- 无外网：先在能上网的机器上 `npm install`，把整个 `node_modules/` 连同项目一起拷过去
- 要本地编译：`sudo apt-get install -y build-essential python3`

**2. 网卡探测已针对 Linux 加固（`src/net.js`）**

Linux 上 `os.networkInterfaces()` 会返回 `docker0` / `br-*` / `veth*` / `virbr0` / `tailscale0` 等虚拟网卡。若直接取第一个，二维码可能指向 `172.17.0.1` 这类容器网桥，**评委手机根本连不上**。

现在按「真实网卡优先 → 常见网段（192.168 > 10 > 172.16–31）优先」排序，并在后台下拉框里显示网卡名、对虚拟网卡给出警告。

**3. 防火墙换成 firewalld / ufw**

Windows 上是放行入站规则；Linux 上通常是：

```bash
sudo ufw allow 3000/tcp          # Ubuntu / Debian
sudo firewall-cmd --add-port=3000/tcp --permanent && sudo firewall-cmd --reload   # RHEL / CentOS
```

`start.sh` 会检测 ufw 是否启用、端口是否放行，并给出提示。

> 端口用 3000 不需要 root（>1024）；若改成 80 需要 root，且代码已对 `EACCES` 给出提示。

---

## 9. 实现陷阱清单（按优先级）

1. ⚠️ **`ballot` / `score` 必须 `WITHOUT ROWID`** —— 否则插入顺序泄露身份（§5.2）
2. ⚠️ **评分控件绝不允许默认值** —— 否则「必须全评」校验形同虚设
3. ⚠️ **排名用整数 `U_c`，禁用浮点比较** —— 否则出现「显示同分、名次不同」（§4.5）
4. ⚠️ **一律去一高一低** —— k 恒为 N−2；保底规则已删除（§4.4）
5. ⚠️ **提交的幂等性** —— 靠 `UPDATE ... WHERE status IN (...)` 的影响行数判断，防连点重复提交
6. ⚠️ **已提交链接禁止作废** —— UI 禁用 + 接口拒绝（§6.2）
7. **`opened` 状态幂等** —— 重复打开不覆盖首次 `opened_at`
8. **封盘竞态** —— 封盘后拒绝新提交，已提交的照常计票
9. **备份** —— 不需要报表导出，但必须有下载 `.db` 的能力，否则笔记本一重装全没了
10. **权重校验** —— 整数、合计必须 = 100；已有提交后禁止改维度数与参赛者数

---

## 10. 验收清单

**连通性**

- [ ] 手机连同一 WiFi 能打开 `http://<IP>:3000/v/:code`

**评委端**

- [ ] 打一半刷新 / 关闭浏览器重开 → 草稿完整恢复
- [ ] 只评了 10 个人点提交 → 被拒，且提示缺哪几项
- [ ] 全部评完 → 提交成功，页面转只读
- [ ] 同一链接再次提交 → `409`，前端提示「已提交过」
- [ ] 清空 localStorage 后再次提交 → 同样得到友好提示（不是报错）
- [ ] 作废的链接打开 → 失效页
- [ ] 封盘后提交 → 被拒

**管理端**

- [ ] 生成 20 个链接，列表可复制、可单个作废
- [ ] 已提交链接的作废按钮为禁用态
- [ ] 进度区正确区分 `未打开 / 已打开未提交 / 已提交 / 已作废`
- [ ] 未登录访问管理接口 → 拒绝

**计分（用造的数据验证）**

- [ ] `N = 5` 与 `N = 6` 都去一高一低（k 分别为 3 和 4）
- [ ] `N = 0` → 显示「数据不足」
- [ ] 构造并列总分 → 名次跳号正确（如 `[1, 2, 2, 4]`）
- [ ] 手算一组小样本，与系统结果逐位核对

**匿名性**

- [ ] `ballot` 表结构无 `code` 列、无时间戳列
- [ ] `ballot` / `score` 建表语句含 `WITHOUT ROWID`
- [ ] 数据库文件里找不到任何「选票 ↔ 邀请码」的对应痕迹

---

## 11. 明确排除的范围

| 项 | 原因 |
|---|---|
| 数据报表导出（Excel/CSV） | 已确认不需要，看板够用（保留 `.db` 备份能力） |
| 多场活动并存与历史回看 | 已确认只支持一场 |
| ~~二维码生成~~ | **2026-09-16 已改为「固定二维码入口 + 自助领取」**，见 §6.3 |
| 设备指纹 / 按浏览器识别评委身份 | 不可靠（同型号手机 UA 相同），且任何能识别身份的信息写进选票就会破坏匿名性（§5.2） |
| 防刷票（IP 限制 / 验证码 / 设备指纹） | 内网同事场景，靠链接不外传 |
| HTTPS / 域名 / 反向代理 | 内网 HTTP 单机 |
| 提交后修改（`ballot_token` 方案） | 已确认提交即终局 |
| 弃权 / 未观看 | 已确认必须全部打分 |

---

## 12. 待办与开放风险

- [ ] **完成 §2 的 Day-0 连通性验证**（唯一阻断项）
- [x] ~~落实 §4.4 的 `N < 6` 保底规则~~ → 2026-09-16 该规则已按需求**删除**，改为一律去分；N ≤ 2 的边界后果已确认接受
- [ ] 落实 §7.3 的「已打开未提交」状态，用于截止前催票
- [ ] 管理员口令强度与存放方式（建议哈希后存 `setting` 表，首启随机生成并打印）
- [ ] 向评委发布「评分须知」：说明**提交后不可修改**、**必须全部打分**、**请基于实际了解打分**（技术手段解决不了编造分，只能靠说明）
